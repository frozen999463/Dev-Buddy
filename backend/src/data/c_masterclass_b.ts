// C Masterclass — Part B (Sections 6-7)
export const cMasterclassPartB = [
    {
        title: "Arrays and Strings", order: 6,
        chapters: [
            {
                title: "Memory Blocks and Character Sequences", order: 1,
                nodes: [
                    {
                        title: "Introduction to Arrays", type: "lesson", order: 1,
                        content: `## Introduction to Arrays\n\n### Collections of Data\nIf you are writing software to track the final grades of 100 university students, creating 100 separate integer variables—\`int grade1, grade2, grade3...\`—is completely impossible to maintain. Programming requires mechanisms to store massive collections of similar data logically and efficiently. \n\nIn C, the foundational collection data structure is the **Array**. An array is a contiguous block of physical RAM allocated explicitly to store multiple items of the *exact same data type* right next to each other sequentially.\n\n### Declaring and Initializing Arrays\nTo create an array, you state the data type, the variable name, and you strictly append square brackets \`[]\` containing the exact, permanent maximum size of the array.\n\n\`\`\`c\n// We are reserving a rigid block of RAM meant to hold exactly 5 integers.\nint student_scores[5];\n\n// We can initialize an array immediately utilizing curly braces:\nint high_scores[5] = {98, 85, 92, 70, 100};\n\`\`\`\n\n### Array Indexing\nArrays organize their internal compartments mathematically via numerical positions called **indices**. Crucially, exactly like Python or Java, C arrays are zero-indexed! The very first compartment is locked at index \`0\`, and the absolute last compartment of an array sized 5 is locked at index \`4\`.\n\n\`\`\`c\nint high_scores[5] = {98, 85, 92, 70, 100};\n\nprintf("The top student got: %d\\n", high_scores[0]); // Prints 98\n\n// Overwriting the third compartment (Index 2)\nhigh_scores[2] = 50;\n\`\`\`\n\n### The Deadly Out-of-Bounds Error\nThe most critical engineering paradigm to remember when coding arrays in C is that the compiler offers absolutely **zero bounds checking**. If you construct an array of size 5, and then attempt to aggressively force data into index 500 (\`high_scores[500] = 99;\`), the compiler will NOT automatically stop you! It will blindly reach 500 memory slots past the array and violently overwrite whatever random data exists there. This immediately causes a catastrophic software segmentation fault or introduces a massive security vulnerability into your application. You are completely manually responsible for policing your own array limits.`
                    },
                    {
                        title: "Strings as Character Arrays", type: "lesson", order: 2,
                        content: `## Strings as Character Arrays\n\n### The Absence of a String Data Type\nThis fact routinely shocks beginners: **The C language inherently possesses no "String" data type.** There is absolutely no built-in \`string\` primitive like there is in Python or JavaScript. C only natively understands the \`char\` structure—a singular, isolated byte representing exactly one letter.\n\nSo how do we type "Hello World"? We engineer it from scratch! In C, a string is nothing more than a Sequential Array of \`char\` variables.\n\n\`\`\`c\n// Constructing a "String" character by character manually\nchar greeting[6] = {'H', 'e', 'l', 'l', 'o', '\\0'};\n\`\`\`\n\n### The Null Terminator (\\0)\nLook very closely at the final character in the array above: \`'\\0'\`. This is explicitly the **Null Terminator**. \nBecause arrays in C do not inherently know where they end in memory, the printing function \`printf\` needs a signal to stop reading RAM. The Null Terminator acts as a giant stop sign. The moment \`printf\` strikes a Null Terminator in a character array, it immediately halts string printing, successfully averting reading random garbage memory past the array's end.\n\n**Rule:** A C string array must always logically possess at minimum one extra index slot specifically reserved for the Null Terminator!\n\n### Syntactic Sugar\nConstructing strings letter-by-letter is brutal. Recognizing this, C compilers provide "Syntactic Sugar" (a shortcut). You can initialize a string array utilizing double quotes, and the compiler will violently construct the character array and dynamically jam the Null Terminator onto the very end for you entirely in the background.\n\n\`\`\`c\n// The compiler automatically sizes this array to 6! (5 for Hello + 1 for \\0)\nchar auto_greeting[] = "Hello"; \n\n// Printing a string requires the %s format specifier!\nprintf("%s, User!\\n", auto_greeting);\n\`\`\`\nHowever, recognize that this string remains totally locked at a maximum capacity of 6 bytes. You mathematically cannot dynamically push extra characters into it later without corrupting the surrounding RAM data blocks.`
                    },
                    {
                        title: "The <string.h> Library", type: "lesson", order: 3,
                        content: `## The <string.h> Library\n\n### Manipulating Memory Manually\nBecause strings in C are truly just raw Memory Arrays, you mathematically cannot interact with them the same intuitive way you do in high-level languages. You cannot evaluate \`if (string1 == string2)\` because you would literally just be asking the compiler if the two strings occupy the exact identical physical RAM address block—which they do not. \n\nYou cannot concatenate strings utilizing \`string1 + string2\` either, because you mathematically cannot add static array memory blocks together with basic arithmetic.\n\nTo conduct string engineering, you must import the explicit Standard String Library at the absolute top of your C file: \`#include <string.h>\`.\n\n### Core Operations\n\n**1. strlen() - String Length**\nThis function iteratively scans the character array from the beginning until it violently strikes the \`'\\0'\` Null Terminator. It then returns the exact count of visible characters encountered.\n\`\`\`c\nchar password[] = "SuperSecret";\nint length = strlen(password); // Returns 11\n\`\`\`\n\n**2. strcpy() - String Copying**\nIf you execute \`string_a = string_b\`, the compiler crashes violently, because you cannot reassign an entire array structure dynamically. To copy characters from the source into the destination, utilize \`strcpy()\`.\n\`\`\`c\nchar storage[50];\n// Copies the data from the right into the array on the left.\nstrcpy(storage, "Initial Data Set Completed."); \n\`\`\`\n\n**3. strcmp() - String Comparison**\nTo check if "Password" matches "Password", you deploy \`strcmp()\`. This function evaluates the arrays character by character mathematically. \n\nIf the two strings are absolutely identical, \`strcmp\` returns **0** (which mathematically evaluates to False, making this confusing for modern developers). If they differ, it returns the mathematical difference between the first diverging characters. \n\`\`\`c\nchar input[] = "apple";\nchar target[] = "apple";\n\n// We must check if the difference is strictly 0 to prove equality!\nif (strcmp(input, target) == 0) {\n    printf("The strings perfectly match!\\n");\n}\n\`\`\``
          },
            {
                title: "Arrays and Strings Quiz", type: "quiz", order: 4,
                questions: [
                    { question: "What occurs if you construct an array sized at 5, but then execute `my_array[10] = 50;` in your code?", options: ["The compiler automatically enlarges the array container to capacity 11", "C automatically throws a helpful Index Out of Bounds Error and crashes gracefully", "The compiler violently overwrites whatever random memory block happens to sit 10 spaces away in raw RAM, causing catastrophic silent corruption or a segmentation fault", "The integer is deleted"], correctAnswer: 2 },
                    { question: "Fundamentally, what is a String in the C Programming Language?", options: ["An explicit core primitive specifically designed for extensive text formatting", "A hyper-optimized object instance", "Specifically a contiguous memory Array of `char` datatypes", "A dynamically expanding database entry"], correctAnswer: 2 },
                    { question: "Why is the Null Terminator (`\\0`) an absolutely non-negotiable requirement for C Strings?", options: ["It encrypts the text from hackers", "It strictly acts as a critical stop sign, informing functions like `printf` exactly where the memory block ends so they don't print random garbage memory bytes", "It compresses the string to save processor resources", "Because it signals the compiler to start loop execution"], correctAnswer: 1 },
                    { question: "If you need to evaluate if a user's typed string matches a specific password exactly, which operation from `<string.h>` is mandated?", options: ["string == target", "strcmp()", "strlen()", "strcpy()"], correctAnswer: 1 }
                ]
            }
        ]
    },
    {
        title: "Mini Projects — Arrays", order: 2,
        nodes: [
            {
                title: "Project Guide: Palindrome Detector", type: "lesson", order: 1,
                content: `## Project Guide: Palindrome Detector System\n\n### Introduction\nA fundamental computer science problem utilized in systems engineering is reversing an array sequence seamlessly without creating a secondary array memory footprint. In this project, you will build an analytical tool that tests if a string is a Palindrome (reads identically forwards and backward, like "racecar") strictly by manipulating array index targets.\n\n### Goal\nConstruct a CLI application utilizing \`<string.h>\` that dynamically receives a string sequence from the user. Calculate its explicit mathematical length using \`strlen()\`. Build a \`for\` loop that iterates from both extremities of the array simultaneously towards the exact middle, verifying if the characters perfectly mirror one another. Output a boolean-style status string evaluating the result.\n\n### Requirements\n1. Ensure imports include \`<stdio.h>\` and \`<string.h>\`.\n2. Instantiate a \`char word[100];\` array container to intercept the user input securely.\n3. Utilize \`scanf("%s", word); \` (Notice we explicitly drop the \` & \` operator here! In C, an array's name already secretly acts as a foundational memory Pointer to its absolute first index [0], so the \` & \` is completely redundant!).\n4. Establish \`int length = strlen(word); \`.\n5. Establish an integer tracking flag \`int is_palindrome = 1; \` acting identically to a True/False boolean.\n6. Deploy a \`for\` loop bounding exactly halfway through the string: \`for (int i = 0; i < length / 2; i++)\`.\n7. Cross-examine the opposing characters conditionally.\n\n### Step-by-Step Guide\n\n**Step 1: The Input Engine**\nConstruct your \`main()\` framework. Declare your large 100-character container string. \nPrompt the user utilizing \`printf\`. Intercept typing using \`scanf("%s", word); \`.\n\n**Step 2: Securing Dimensions**\nDemand the array length utilizing \`int len = strlen(word); \`.\n\n**Step 3: Dual Pointer Loop Mechanics**\nInitialize \`int is_palindrome = 1; \`. Initiate the loop: \`for (int i = 0; i < len / 2; i++)\`. Why stop at the middle? Because if you traverse the entire array, you just swap the verified characters twice and ruin your answer!\nInside the execution block, deploy your index-checking architecture: \n\`if (word[i] != word[len - 1 - i])\`.\nThe expression \`len - 1 - i\` dynamically targets the precise inverse mirror character taking into account the \`0\` starting index constraint.\nIf a single mismatch triggers this condition, instantly overwrite your tracking flag \`is_palindrome = 0; \` and explicitly deploy a \`break; \` command to aggressively terminate the loop evaluating any further.\n\n**Step 4: Output Rendering**\nConstruct an \`if (is_palindrome == 1)\` architecture and output the final aesthetic evaluation: "It is a palindrome!" or "It is distinctly NOT a palindrome."\n\n### Challenge Yourself\n- Construct a \`lower_case_converter(char str[])\` external function leveraging an internal loop that manipulates ASCII numerical values, converting all uppercase letters into lowercase beforehand so your detector successfully evaluates "Racecar" despite the capital R.`
          }
        ]
      }
    ]
  },

{
    title: "Structs and Custom Data Types", order: 7,
        chapters: [
            {
                title: "Engineering Complex Structures", order: 1,
                nodes: [
                    {
                        title: "Defining Structs", type: "lesson", order: 1,
                        content: `## Defining Structs\n\n### The Need for Unified Data\nWe've mastered Arrays, which group massive collections of identical data types logically. But what if the data isn't identical? If you are programming a Video Game Player, you need to track their \`char name[50]\`, their \`int health_points\`, and their \`float movement_speed\`. Because arrays strictly mandate uniform data types, you cannot store these together easily.\n\nSo you create three entirely detached variables. If your game expands to include 20 players, you now maintain 60 completely disconnected arrays. Software engineering fundamentally collapses under this disjointed pressure.\n\n### The \`struct\` Framework \nC provides a magnificent modular system to bundle explicitly distinct primitive data types together under one unified architectural namespace. This is incredibly similar to an "Object" or "Class" in Java or Python, except C structures strictly encapsulate raw data—they mathematically cannot contain embedded functional methods!\n\n\`\`\`c\n// Defining the Blueprint (Usually outside of main at the top of the file)\nstruct Player {\n    char name[50];\n    int health;\n    float speed;\n};\n\`\`\`\nNotice the mandatory terminating semicolon \`;\` at the conclusion of the structural block. We have successfully architected a completely custom data primitive called a \`struct Player\`.\n\n### Instantiating and Accessing Structs\nYou can now dynamically spawn instances leveraging your blueprint inside your \`main\` function. To access or alter internal data compartments independently, you deploy the critical "Dot Operator" (\`.\`).\n\n\`\`\`c\nint main() {\n    // Creating an object from our custom blueprint\n    struct Player hero1;\n    \n    // Accessing the internal compartments safely utilizing the Dot Operator\n    hero1.health = 100;\n    hero1.speed = 1.5f;\n    \n    // Remember, arrays must use strcpy, you cannot reassign strings directly!\n    strcpy(hero1.name, "Arthas");\n    \n    printf("Hero Name: %s\\n", hero1.name);\n    printf("Current HP: %d\\n", hero1.health);\n    \n    return 0;\n}\n\`\`\`\nBy grouping explicitly related metrics firmly inside \`struct\` blueprints, you massively elevate the readability, stability, and logical maintainability of your software systems.`
                    },
                    {
                        title: "Arrays of Structs and Struct Pointers", type: "lesson", order: 2,
                        content: `## Arrays of Structs and Struct Pointers\n\n### Scaling the Architecture\nNow that you possess a unified \`struct Player\`, maintaining 20 distinct users no longer requires 60 chaotic variables. Because your \`struct\` is now technically a valid fundamental data type blueprint recognized securely by the compiler, you can declare a unified Array of Structs!\n\n\`\`\`c\n// Declaring an array capable of firmly holding 20 distinct Player structs!\nstruct Player server_roster[20];\n\n// Target Player #1 (Index 0) and directly inject his specific health\nserver_roster[0].health = 100;\n\n// Target Player #2 (Index 1) and modify his internal speed metric\nserver_roster[1].speed = 5.5f;\n\`\`\`\nThis modular approach is exactly how enterprise databases organize structured, disparate data natively in raw memory.\n\n### Passing Structs to Functions\nIf you want to construct a function that aggressively heals a player, you must be hyper-vigilant. \nIf you define \`void heal_player(struct Player p)\`, you are invoking Pass-by-Value mechanics! C will violently copy the entire struct (the massive string array, the integer, the floating point) into a completely isolated RAM footprint for the function, execute the math, and instantly erase the container when finished. It is astronomically slow, consumes double the RAM footprint momentarily, and unequivocally fails to modify the primary \`main()\` player struct!\n\n### Struct Pointers and the Arrow Operator\nYou must utilize exceptionally critical Pass-by-Reference architectures (Pointers) to mutate existing struct entities natively. \nPass the explicit Memory Address of the \`struct\` directly into your function!\n\n\`\`\`c\n// The function expressly demands a Pointer targeting a Player Struct!\nvoid heal_player(struct Player *p_target) {\n    // The Arrow Operator (->) is essential!\n    p_target->health = 100;\n}\n\`\`\`\n\n### The Critical Arrow (->) Operator\nWhen dealing directly with Structs, you utilize the basic Dot Operator (\`.\`). However, if you are explicitly grappling with a *Pointer* that aims at a Struct, you mathematically cannot use the dot. \nYou are mandated to deploy the Arrow Operator (\` -> \`). This specialized operator securely commands the compiler to traverse via the pointer specifically, dereference the underlying structural object automatically, and violently manipulate the targeted compartment metric. \n\n\`\`\`c\nint main() {\n    struct Player hero;\n    // Hand off the physical memory coordinate specifically using &\n    heal_player(&hero); \n}\n\`\`\``
          },
            {
                title: "Typedef and Aliases", type: "lesson", order: 3,
                content: `## Typedef and Code Aliases\n\n### The Verbosity Issue\nStructs are structurally brilliant, but syntactically absolutely exhausting. Every single time you want to declare a container utilizing your custom blueprint, you are strictly mandated to aggressively type the word explicitly: \`struct Player\`. \n\nIf you write a massively complex system, you might declare \`struct Player player1\`, \`struct GameEngine main_system\`, \`struct RenderWindow window1\`. Writing the word \`struct\` hundreds of times inflates typing bandwidth unnecessarily.\n\n### Streamlining Architecture with \`typedef\`\nThe C standard provides a wildly powerful keyword called \`typedef\`. This command effectively allows engineers to violently force the compiler to rename or "Alias" existing data-types into simplified, purely aesthetic labels.\n\n\`\`\`c\n// We aggressively rename 'unsigned long long int' into 'BigNum'\ntypedef unsigned long long int BigNum;\n\n// Now we can use the simplified aesthetic seamlessly\nBigNum factorial_target = 10000;\n\`\`\`\n\n### Combining \`typedef\` explicitly with Structs\nThe ultimate utilization of the keyword acts directly upon \`struct\` declarations. By wrapping \`typedef\` around your standard \`struct\` blueprint matrix, you eradicate the need to eternally prepend your variable declarations identically.\n\n\`\`\`c\n// We explicitly wrap the struct definition. \n// The blueprint is structurally anonymous internally.\ntypedef struct {\n    char title[100];\n    int runtime_minutes;\n    float rating;\n} Movie; // The Alias strictly concludes the statement prior to the semicolon!\n\`\`\`\n\nNow observe the aggressive elegance of initializing your variables organically inside \`main()\`!\n\n\`\`\`c\nint main() {\n    // No messy struct keywords required!\n    Movie jaws;\n    jaws.runtime_minutes = 124;\n    jaws.rating = 8.1f;\n    strcpy(jaws.title, "Jaws");\n    \n    printf("%s boasts a rating of %.1f\\n", jaws.title, jaws.rating);\n    return 0;\n}\n\`\`\`\nAlmost profoundly mimicking the object initialization architecture found in hyper-modern Object-Oriented languages natively.`
          },
{
    title: "Structs Quiz", type: "quiz", order: 4,
        questions: [
            { question: "Fundamentally, what is the precise architectural purpose of a C `struct`?", options: ["To rigorously define explicit mathematical functions structurally", "To dynamically format unstructured output explicitly into JSON sequences", "To bundle entirely diverse primitive data variables under one logically consolidated blueprint namespace explicitly", "To securely encrypt an entire character array violently"], correctAnswer: 2 },
            { question: "Which mechanical operator natively grants explicit internal access to data compartments originating from a standard Struct instance?", options: ["The Arrow `->` Operator", "The Ampersand `&` Operator", "The Dot `.` Operator", "The Dereference `*` Operator"], correctAnswer: 2 },
            { question: "If a function fundamentally intercepts a Pointer inherently targeting a struct instance explicitly, strictly which operator MUST you deploy to manipulate internal data accurately?", options: ["The Dot `.` Operator", "The Arrow `->` Operator", "The `new` Keyword", "The Addition `+` Operator"], correctAnswer: 1 },
            { question: "What explicit syntactical benefit does deploying `typedef` over a struct permanently confer?", options: ["It encrypts the variable seamlessly into byte format", "It enables the deployment of variables natively without identically necessitating the prefix keyword 'struct' every single time", "It dynamically grants the struct inherited abilities explicitly", "It accelerates absolute compile times astronomically"], correctAnswer: 1 }
        ]
}
        ]
      },
{
    title: "Mini Projects — Structs", order: 2,
        nodes: [
            {
                title: "Project Guide: Student Database System", type: "lesson", order: 1,
                content: `## Project Guide: Student Database Roster System\n\n### Introduction\nManaging deeply interrelated datasets strictly segregating names, IDs, and mathematical GPAs using totally isolated disconnected string arrays is an architectural disaster. Building custom Structs structurally rectifies this completely. In this capstone project for Section 7, you will seamlessly consolidate diverse datasets dynamically utilizing structs and an array list sequence to architect a fundamentally sound database console.\n\n### Goal\nConstruct a comprehensive text database deploying a heavily modified \`typedef struct\` designated perfectly as \`Student\`. Instantiate an explicit array of \`Student\` items natively holding 3 independent database registries logically. Utilize loops to explicitly collect input variables dynamically from a user, inject them into the distinct structs safely, and format an exhaustive output matrix displaying the finalized roster cleanly.\n\n### Requirements\n1. Define absolutely a \`typedef struct\` named explicit \`Student\`. Internally equip it with a \`char name[50]; \`, an \`int id_number; \`, and a \`float gpa; \`.\n2. In \`main()\`, allocate a dedicated memory array footprint capable of tightly retaining specifically \`3\` independent instances of the \`Student\` class structurally.\n3. Integrate an autonomous \`for\` loop cycling exactly from 0 to 2.\n4. Within the loop explicitly execute \`printf()\` soliciting input logically. Execute \`scanf()\` rigorously pushing variables strictly into the specific index struct attributes directly! (e.g., \` & database[i].gpa\`).\n5. Generate a secondary subsequent \`for\` loop dynamically reading the populated arrays out iteratively, rendering an aesthetically pleasing formatted receipt layout.\n\n### Step-by-Step Guide\n\n**Step 1: Architecting the Foundation**\nAt the peak of formatting, define the struct globally dynamically wrapping it logically in typedef. \n\`typedef struct { char name[50]; int id; float gpa; } Student; \` \nInstantiate the active array in \`main\`: \`Student roster[3]; \`\n\n**Step 2: The Data Acquisition Engine**\nBuild the primary acquisition loop: \`for (int i = 0; i < 3; i++)\`. \nPrompt explicitly: \`printf(\"Enter Name for Student %d: \", i+1);\` \nTarget the string: \`scanf(\"%s\", roster[i].name);\` (Remember strings inherently do strictly not require the ampersand \`&\` natively format!) \nTarget integers fundamentally: \`scanf(\"%d\", &roster[i].id);\` (Requires explicitly the Address Operator!).\nExecute for exactly the float structure natively similarly.\n\n**Step 3: Rendering the Output Sequence**\nConclude the input loops seamlessly. Implement a clear visual boundary like \`printf(\"------ ROSTER ------\\n\");\`. \nArchitect a secondary exactly duplicate loop traversing 0 to 2. Execute a formatted multi-variable extraction explicitly outputting the names and IDs symmetrically. Format the float string identically executing \`%.2f\` for GPA formatting rendering.\n\n### Challenge Yourself\n- Construct an independent dedicated external function explicitly titled \`void print_student(Student *s)\` expecting a struct POINTER structure exclusively. Rip the rendering \`printf\` out of your secondary loop structurally, and dynamically invoke your new custom external function sending the Memory Address directly: \`print_student(&roster[i]);\`!`
          }
        ]
      }
    ]
  }
];
