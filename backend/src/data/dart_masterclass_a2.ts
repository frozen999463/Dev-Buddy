// Dart Masterclass — Part A2 (Sections 4-5)
export const dartMasterclassPartA2 = [
  {
    title: "Control Flow", order: 4,
    chapters: [
      {
        title: "Conditional Decisions and Logical Branching", order: 1,
        nodes: [
          {
            title: "If, Else, and the Ternary Operator", type: "lesson", order: 1,
            content: `## If, Else, and the Ternary Operator\n\n### Making Logic Choices\nA script that only runs in a straight line is just a list of instructions. A true "Program" is an entity capable of making decisions based on changing data. In Dart, we use Conditional Statements to tell the computer: "If this condition is true, execute this specific block of code; otherwise, do something else." This is how you build everything from a simple login screen to complex AI behavior.\n\n### The If-Else Stack\nThe most common decision-making tool is the \`if\` statement. It evaluates a boolean expression (a question that results in either \`true\` or \`false\`).\n\n\`\`\`dart\nint health = 50;\n\nif (health > 80) {\n  print('Character is Healthy');\n} else if (health > 20) {\n  print('Character is Injured');\n} else {\n  print('Character is Critical!');\n}\n\`\`\`\nIn Dart, curly braces \`{}\` are technically optional for single-line statements, but professional developers *always* use them. It makes the code much easier to read and prevents accidental bugs when adding more lines later.\n\n### The Ternary Operator\nSometimes, an \`if-else\` block is too bulky for a simple decision. For example, if you just want to decide which word to show based on a boolean, you can use the **Ternary Operator** (\`? :\`). It is a condensed, one-line version of an \`if-else\` statement.\n\n\`\`\`dart\nbool isDarkMode = true;\nString uiColor = isDarkMode ? 'Black' : 'White';\n// Logic: Condition ? Result_If_True : Result_If_False\n\`\`\`\nThis is incredibly common in Flutter development where you often need to pick a design style based on a single condition. It keeps your code tidy and readable while still providing powerful logical flow control.`
          },
          {
            title: "Switch Statements and Enums", type: "lesson", order: 2,
            content: `## Switch Statements and Enums\n\n### Handling Multiple States\nIf you have a variable that can be one of many distinct values (like the status of an order: 'Pending', 'Shipped', 'Delivered'), using ten \`if-else\` statements in a row is exhausting. Dart provides the \`switch\` statement as a cleaner and more efficient way to handle "multiple choice" logic.\n\n### The Switch Structure\nA switch statement takes a single variable and compares it against multiple \`case\` options. When a match is found, it executes that specific block until it hits the \`break\` keyword.\n\n\`\`\`dart\nString weather = 'sunny';\n\nswitch (weather) {\n  case 'sunny':\n    print('Wear a t-shirt');\n    break;\n  case 'rainy':\n    print('Bring an umbrella');\n    break;\n  default:\n    print('Check the forecast');\n}\n\`\`\`\n\n### Power Up with Enums\nUsing strings like \`'sunny'\` is dangerous because you might accidentally type \`'suny'\` and the code will fail silently. To fix this, we use **Enums** (short for Enumerations). An Enum is a custom data type that represents a fixed set of constant values. It makes your code "Type Safe."\n\n\`\`\`dart\nenum WeatherCondition { sunny, rainy, cloudy, snowy }\n\nvoid main() {\n  var current = WeatherCondition.sunny;\n  \n  switch (current) {\n    case WeatherCondition.sunny:\n      print('Enjoy the sun!');\n      break;\n    // The compiler will WARN you if you forget to handle a case!\n  }\n}\n\`\`\`\nWhen you use a switch with an Enum, Dart is smart enough to tell you if you've forgotten to handle one of the cases. This is a massive safety net for building large, complex applications.`
          },
          {
            title: "Loops: For, While, and Do-While", type: "lesson", order: 3,
            content: `## Loops: For, While, and Do-While\n\n### Automating Repetitive Tasks\nA computer is essentially a machine that is incredible at doing the same thing over and over again without getting bored. In programming, we use **Loops** to automate these repetitive tasks. Whether you are printing 100 receipts or scanning a list of a million users, loops are your best friend.\n\n### 1. The \`for\` Loop\nThis is the most common loop. Use it when you know exactly how many times you want to repeat something.\n\`\`\`dart\nfor (int i = 0; i < 5; i++) {\n  print('Iteration $i');\n}\n\`\`\`\n\n### 2. The \`while\` Loop\nUse this when you don't know the exact count, but you want to keep going as long as a certain condition remains true. It checks the condition **before** running the code.\n\`\`\`dart\nbool isBatteryLow = false;\nwhile (!isBatteryLow) {\n  print('Keep phone on');\n  // if (voltage < 5) isBatteryLow = true;\n}\n\`\`\`\n\n### 3. The \`do-while\` Loop\nThis is similar to the while loop, but it guarantees the code runs **at least once**, because it checks the condition at the end of the block instead of the beginning.\n\n### Controlling the Loop: Break and Continue\n- **\`break\`**: Immediately kills the loop and jumps out of it entirely.\n- **\`continue\`**: Skips the rest of the current iteration and jumps straight to the next cycle.\n\nExample: If you are searching for a specific user in a list of 1,000 people, the moment you find them, you should call \`break\` so the computer doesn't waste time checking the other 999 people!`
          },
          {
            title: "Control Flow Quiz", type: "quiz", order: 4,
            questions: [
              { question: "Which conditional operator is a one-line alternative to an if-else statement?", options: ["The modulo operator (%)", "The ternary operator (? :)", "The null-check operator (?)", "The arrow operator (=>)"], correctAnswer: 1 },
              { question: "What is the primary architectural advantage of using an 'Enum' with a switch statement?", options: ["It makes the code run faster", "It provides type safety, ensuring only valid options are used and warning you if a case is missed", "It allows the switch to handle numbers", "It automatically translates the code to JavaScript"], correctAnswer: 1 },
              { question: "Which loop guarantees that the code inside will run at least once?", options: ["for", "while", "do-while", "for-in"], correctAnswer: 2 },
              { question: "What does the 'continue' keyword do inside a loop?", options: ["It stops the loop and exits the program", "It skips the current iteration and moves immediately to the next one", "It restarts the loop from zero", "It pauses the loop for one second"], correctAnswer: 1 }
            ]
          }
        ]
      },
      {
        title: "Mini Projects — Control Flow", order: 2,
        nodes: [
          {
            title: "Project Guide: Guess the Number Game", type: "lesson", order: 1,
            content: `## Project Guide: Guess the Number Game\n\n### Introduction\nIn this project, you will use your knowledge of loops and if-else statements to build a classic "Guess the Secret Number" game. This project will teach you how to maintain "state" (keeping track of attempts) and how to build an interactive feedback loop with a user.\n\n### Goal\nCreate a program that has a hardcoded \`targetNumber\` (e.g. 42). Use a loop to give the user 5 attempts to guess the number. After each guess, tell them if they are "Too High", "Too Low", or "Correct!". If they win, stop the loop early. If they run out of turns, tell them they lost.\n\n### Requirements\n1. Define a \`const targetNumber = 42;\`.\n2. Use a \`for\` loop to track 5 attempts.\n3. Create a variable \`bool hasWon = false;\`.\n4. Inside the loop, prompt for a guess. For now, you can simulate guesses by iterating through a list or using a hardcoded variable.\n5. Use an \`if-else if-else\` block to compare the guess to the target.\n6. If the guess is correct, set \`hasWon = true;\` and use \`break;\` to exit the loop instantly.\n7. After the loop, check the \`hasWon\` variable and print a final victory or defeat message.\n\n### Step-by-Step Guide\n\n**Step 1: Variables**\nDefine your target: \`int target = 7;\`. Define a flag: \`bool win = false;\`.\n\n**Step 2: The Loop**\nStart a loop: \`for (int i = 1; i <= 5; i++) { ... }\`. This represents five distinct turns.\n\n**Step 3: The Logic**\nSimulate a guess (e.g. \`int guess = 5;\`). Compare it: \`if (guess < target) { print('Too Low!'); }\` and so on.\n\n**Step 4: Winning**\nIf \`guess == target\`, execute: \`print('You got it!'); win = true; break;\`. The \`break\` is critical! You don't want to keep guessing after you've already won.\n\n**Step 5: Final Check**\nOutside the loop: \`if (!win) { print('Game Over. The number was $target'); }\`.\n\n### Challenge Yourself\n- Can you count how many attempts the user took to win and display that in the final message? (Hint: Use the loop variable \`i\`).\n- Use \`import 'dart:math';\` and a random number generator (\`Random().nextInt(100)\`) to make the secret number different every time you run the app!`
          }
        ]
      }
    ]
  },

  {
    title: "Functions and Modularity", order: 5,
    chapters: [
      {
        title: "Function Mastery", order: 1,
        nodes: [
          {
            title: "Defining and Invoking Functions", type: "lesson", order: 1,
            content: `## Defining and Invoking Functions\n\n### The Power of Reusability\nA function is a self-contained block of code that performs a specific task. Imagine you frequently need to calculate the tax on a price. Instead of writing the math formula 50 times in your app, you write it once inside a function and "call" it whenever you need it. This is the cornerstone of **DRY** programming—"Don't Repeat Yourself."\n\n### Anatomy of a Dart Function\nA standard Dart function has four main parts:\n1. **Return Type:** What kind of data comes back (int, String, void).\n2. **Name:** A unique identifier (usually camelCase).\n3. **Parameters:** Inputs required to do the work, enclosed in \`()\`. \n4. **Body:** The actual code, enclosed in \`{}\`.\n\n\`\`\`dart\ndouble calculateTotal(double price, double taxRate) {\n  return price + (price * taxRate);\n}\n\nvoid main() {\n  // Calling the function and storing the result\n  double finalPrice = calculateTotal(100.0, 0.08);\n  print('Final: $finalPrice');\n}\n\`\`\`\n\n### Arrow Syntax (Short-Hand)\nIf your function is very simple and only has one single line of code, you can use the "Fat Arrow" (\`=>\`) syntax to make it much more compact. This is extremely common in Flutter.\n\n\`\`\`dart\n// Long way\nint add(int a, int b) { return a + b; }\n\n// Fast arrow way\nint add(int a, int b) => a + b;\n\`\`\`\nThis doesn't change how the function works; it just makes your code look cleaner and more professional. You don't need the \`return\` keyword or the curly braces when using the arrow syntax.`
          },
          {
            title: "Parameters: Required vs. Optional", type: "lesson", order: 2,
            content: `## Parameters: Required vs. Optional\n\n### Flexibility in Inputs\nIn some languages, you must provide every input exactly in order. Dart is much more flexible. It allows you to create functions with Optional and Named parameters, making your code significantly easier to read when functions grow complex.\n\n### 1. Positional Parameters (The Default)\nThese must be provided in the exact order they are defined.\n\`void greet(String name, String title) { ... }\` \nCall: \`greet('Bob', 'Manager');\`\n\n### 2. Optional Positional Parameters\nBy wrapping parameters in square brackets \`[]\`, you make them optional. If the user doesn't provide them, they become \`null\`.\n\`void sayHello(String name, [String? suffix]) { ... }\` \nCall: \`sayHello('Kevin');\` or \`sayHello('Kevin', 'Jr');\`\n\n### 3. Named Parameters (Recommended)\nBy wrapping parameters in curly braces \`{}\`, you enable **Named Parameters**. These are the "Gold Standard" in Flutter. They allow you to pass arguments in any order by specifying their names. They are also much clearer to read.\n\n\`\`\`dart\nvoid createUser({required String name, int age = 18}) {\n  print('$name is $age years old.');\n}\n\nvoid main() {\n  // Usage is extremely clear!\n  createUser(name: 'Alice', age: 25);\n  createUser(name: 'Charlie'); // Uses default age of 18\n}\n\`\`\`\nUsing \`required\` ensures the developer doesn't forget vital information, while default values (\`age = 18\`) provide a fallback if they do.`
          },
          {
            title: "Lexical Scope and Closures", type: "lesson", order: 3,
            content: `## Lexical Scope and Closures\n\n### Understanding the Walls\nIn programming, **Scope** determines where a variable is accessible. In Dart, scope is defined by curly braces \`{}\`. Think of braces as "invisible walls" that protect your variables from being messed with by the rest of the app.\n\n1. **Global Scope:** Variables defined outside of any function can be seen by everything.\n2. **Local Scope:** Variables defined inside a function or a loop can only be seen *inside* that specific block. Once the function finishes running, the local variable is destroyed to save memory.\n\n### Nested Scopes\nOuter functions cannot see inside inner functions, but inner functions **can** see variables in the outer functions that contain them. This is called "Lexical Scoping."\n\n### What is a Closure?\nA **Closure** is a special type of function that "remembers" the variables in its parent scope even after the parent function has finished running. This lives in your app's memory and acts like a private container for data.\n\n\`\`\`dart\nFunction makeCounter() {\n  int count = 0; // This variable lives in a closure\n  \n  return () {\n    count++;\n    print('Count: $count');\n  };\n}\n\nvoid main() {\n  var myCounter = makeCounter();\n  myCounter(); // Count: 1\n  myCounter(); // Count: 2\n}\n\`\`\`\nThis pattern is incredibly useful for creating "Private" variables that can't be touched by the outside world, ensuring your data stays safe and your app stays bug-free.`
          },
          {
            title: "Functions Quiz", type: "quiz", order: 4,
            questions: [
              { question: "What does the 'void' return type signify in a function signature?", options: ["The function returns a string", "The function executes code but returns no mathematical data", "The function returns zero", "The function is invalid"], correctAnswer: 1 },
              { question: "When should you use the 'Fat Arrow' (=>) syntax?", options: ["When a function has many parameters", "When a function has a single line of code and returns a value", "Only in the main function", "To declare a constant"], correctAnswer: 1 },
              { question: "What is the primary benefit of using Named Parameters ({} syntax)?", options: ["They make the code run twice as fast", "They allow you to pass arguments in any order and make the function calls much easier to read", "They prevent the use of strings", "They allow you to hide the function's name"], correctAnswer: 1 },
              { question: "What determines the 'Scope' of a variable in Dart?", options: ["The variable's data type", "The curly braces {} where the variable is defined", "Whether the variable is final or const", "The file name"], correctAnswer: 1 }
            ]
          }
        ]
      },
      {
        title: "Mini Projects — Functions", order: 2,
        nodes: [
          {
            title: "Project Guide: Area & Perimeter Calculator", type: "lesson", order: 1,
            content: `## Project Guide: Modular Geometry Calculator\n\n### Introduction\nIn this project, you will move away from writing all your code inside \`main()\` and instead build a modular library of functions. This is how real-world software is structured: tiny, specialized functions working together to solve a larger problem.\n\n### Goal\nCreate a program that calculates the area and perimeter of multiple shapes (rectangles and circles). You will define separate functions for each calculation and call them from \`main()\` with different inputs to verify their accuracy.\n\n### Requirements\n1. Use \`import 'dart:math';\` at the top to get access to \`pi\`.\n2. Function 1: \`calcRectArea(double l, double w)\` using arrow syntax.\n3. Function 2: \`calcRectPerim(double l, double w)\` using arrow syntax.\n4. Function 3: \`calcCircleArea({required double radius})\` using named parameters.\n5. In \`main()\`, call all functions twice with different numbers.\n6. Format the output to show strictly 2 decimal places using \`.toStringAsFixed(2)\`.\n\n### Step-by-Step Guide\n\n**Step 1: Arrow Functions**\nDefine your rectangle logic as clean one-liners above or below \`main()\`. \n\`double rectArea(double l, double w) => l * w;\`.\n\n**Step 2: Circle Math**\nDefine the circle function using the named parameter syntax to make it clear what the input represents: \`double circArea({required double r}) => pi * r * r;\`.\n\n**Step 3: Execution**\nIn \`main()\`, create standard variables for length, width, and radius. Call your functions: \`double a = rectArea(10, 5);\`. \n\n**Step 4: Interpolation**\nPrint your results using the bracket syntax for method calls: \`print('The area is: \${a.toStringAsFixed(1)}');\`.\n\n**Step 5: Refactoring**\nTry to combine your functions! Could you make one function that takes a "Shape" enum and calculates the area based on which shape is passed? (This is a sneak peek at Advanced OOP!).\n\n### Challenge Yourself\n- Create a "Display Result" function that takes a String label and a double value, and handles all the \`print()\` and \`.toStringAsFixed()\` formatting in one place!\n- Add an optional \`String? unit\` parameter (like 'cm' or 'inches') to your functions. If it's provided, print it next to the result (e.g., "50.0 cm").`
          }
        ]
      }
    ]
  }
];