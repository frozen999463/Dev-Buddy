// Python Masterclass — Part B (Sections 6-7)
export const pythonMasterclassPartB = [
    {
        title: "Advanced Collections and Memory", order: 6,
        chapters: [
            {
                title: "Lists, Tuples, and Sets", order: 1,
                nodes: [
                    {
                        title: "Dynamic Lists and Memory", type: "lesson", order: 1,
                        content: `## Dynamic Lists and Memory\n\n### The Collection Problem\nWe've learned how variables store single pieces of data. But what if you are writing software for a school, and you need to store the names of 500 students? Creating 500 separate variables (\`student1\`, \`student2\`, etc.) is absurd. We solve this using **Collections**—data types that can store multiple items inside a single variable.\n\n### Introduction to Lists\nThe most versatile collection in Python is the **List**. A list is an ordered, mutable (changeable) sequence of items. You create a list by placing comma-separated values inside square brackets \`[]\`.\n\n\`\`\`python\nmy_inventory = ["Sword", "Shield", "Health Potion"]\nwinning_numbers = [4, 8, 15, 16, 23, 42]\n\`\`\`\n\nUnlike languages like C or Java, Python lists are dynamic. They can shrink and grow automatically, and a single list can even hold mixed data types:\n\`\`\`python\nrandom_data = [42, "Hello", True, 3.14]\n\`\`\`\n\n### Indexing and Modifying\nLists are ordered, meaning every item has a specific numerical position called an **index**. Crucially, computer science counts from zero! The first item is at index 0.\n\n\`\`\`python\nheroes = ["Batman", "Superman", "Wonder Woman"]\nprint(heroes[0])  # Batman\nprint(heroes[2])  # Wonder Woman\n\n# Modifying an item\nheroes[1] = "Flash"\nprint(heroes)  # ['Batman', 'Flash', 'Wonder Woman']\n\`\`\`\n\n### Adding and Removing Items\nPython lists offer a wealth of built-in methods.\n- \`.append(item)\` adds a single item to the very end of the list.\n- \`.insert(index, item)\` squeezes an item into a specific position, pushing everything else to the right.\n- \`.remove(item)\` searches for the first instance of a value and deletes it.\n- \`.pop()\` removes and returns the absolute last item in the list.\n\n\`\`\`python\nbackpack = ["Map", "Compass"]\nbackpack.append("Flashlight")\nbackpack.remove("Compass")\n\`\`\`\n\n### Memory and References\nA Python list is an object in memory, and the variable name is just a reference (a pointer) to that memory address. If you do \`list_b = list_a\`, you have NOT created a copy! Both variables point to the same list. Modifying \`list_b\` will change \`list_a\`.\n\nTo make a true isolated copy, use the \`.copy()\` method or slicing syntax \`list_a[:]\`.`
                    },
                    {
                        title: "Tuples and Sets", type: "lesson", order: 2,
                        content: `## Tuples and Sets\n\nWhile lists are fantastic, they are not the only collections available. Sometimes the ability to change a list is actually a liability. What if we have data that must remain absolutely constant?\n\n### Tuples: The Immutable Collection\nA **Tuple** is essentially a list that cannot be changed once it is created. It is **immutable**. You define a tuple using parentheses \`()\` instead of square brackets.\n\n\`\`\`python\n# Creating a tuple\nrgb_red = (255, 0, 0)\ndatabase_credentials = ("admin", "supersecret123")\n\`\`\`\nIf you try to run \`rgb_red[0] = 100\`, Python will throw a \`TypeError\`. The tuple is locked. \nWhy use a tuple? \n1. **Performance:** Tuples are slightly faster and consume less memory than lists.\n2. **Safety:** If you pass a tuple into a complex function, you can guarantee that function won't accidentally delete or alter the data.\n\n### Sets: The Unique Collection\nA **Set** is an unordered collection that cannot contain any duplicate elements. You define a set using curly braces \`{}\`. Sets are heavily optimized for mathematical operations like finding intersections or unions.\n\n\`\`\`python\nvalid_ids = {101, 102, 103, 101, 101}\n# Duplicates are instantly discarded silently\nprint(valid_ids)  # {101, 102, 103}\n\`\`\`\nBecause sets are completely unordered, you cannot use indexing! \`valid_ids[0]\` will crash. \n\n### The Power of Set Lookups\nThe true superpower of a Set is its lookup speed. Behind the scenes, sets use a data structure called a hash table. If you have a list of a million usernames, checking if a specific name is in the list (\`if name in usernames:\`) requires Python to scan the list item by item—which is slow.\nIf you convert that list to a set, Python can check if the name exists almost instantly, regardless of how massive the collection is!\n\n\`\`\`python\nregistered_users = ["alice123", "bob456", "charlie789"]\n# Convert list to a set for blazing fast lookups\nuser_set = set(registered_users)\n\nif "bob456" in user_set:\n    print("User found immediately!")\n\`\`\``
          },
            {
                title: "Dictionaries: Key-Value Pairs", type: "lesson", order: 3,
                content: `## Dictionaries: Key-Value Pairs\n\n### Introduction to Dictionaries\nLists and tuples organize data by numerical index (\`0\`, \`1\`, \`2\`). But often, numbers aren't meaningful. Imagine trying to store a user's profile context in a list: \`["Alice", 28, "Engineer", "alice@email.com"]\`. If you want to find her email, you must remember it is at index 3. \n\nPython provides the **Dictionary** (often called Hash Maps in other languages). Dictionaries store data in **Key-Value pairs**. You look up the data using a meaningful "Key" instead of a number. Dictionaries use curly braces, with a colon separating the key and the value.\n\n\`\`\`python\nuser_profile = {\n    "name": "Alice",\n    "age": 28,\n    "job": "Engineer",\n    "email": "alice@email.com"\n}\n\`\`\`\n\n### Accessing and Modifying Data\nTo get data out of a dictionary, you provide the key inside square brackets.\n\`\`\`python\nprint(user_profile["name"])  # Prints "Alice"\n\`\`\`\nIf you ask for a key that does not exist, Python will throw a \`KeyError\` and crash the program. To avoid this, use the \`.get()\` method, which safely returns \`None\` if the key is missing, or a default value that you specify.\n\`\`\`python\n# Safe access\nprint(user_profile.get("phone", "No phone number found"))\n\`\`\`\n\nAdding or modifying an item is incredibly simple. You just pretend the key already exists and assign a value to it.\n\`\`\`python\n# Modifying existing data\nuser_profile["age"] = 29\n\n# Adding brand new data\nuser_profile["is_active"] = True\n\`\`\`\n\n### Looping Through Dictionaries\nBecause a dictionary has two parts (keys and values), looping over them requires a specific method. The \`.items()\` method returns both the key and the value for every item in the dictionary simultaneously.\n\n\`\`\`python\n# This loop unpacks the key and the value on every iteration\nfor key, value in user_profile.items():\n    print(f"{key.capitalize()}: {value}")\n\`\`\`\nDictionaries are arguably the most important data structure in Python. Every time your program talks to an API on the internet, the data you receive will be structured as JSON, which maps almost perfectly to Python Dictionaries.`
            },
            {
                title: "Collections Quiz", type: "quiz", order: 4,
                questions: [
                    { question: "Which collection is fully immutable (cannot be changed)?", options: ["List", "Set", "Dictionary", "Tuple"], correctAnswer: 3 },
                    { question: "If you assign `list_b = list_a` and then modify `list_b`, what happens?", options: ["Only list_b is modified", "Both lists are modified because they point to the exact same memory address", "Python throws an error", "list_a is deleted"], correctAnswer: 1 },
                    { question: "Which collection is optimal for checking for the existence of an item incredibly fast and destroying duplicates?", options: ["List", "Tuple", "Set", "Array"], correctAnswer: 2 },
                    { question: "How do you safely access a dictionary key without risking a program-crashing KeyError?", options: ["Using brackets `dict['key']`", "Using the `dict.get('key')` method", "Using `dict.fetch('key')`", "Wrapping it in `safe()`"], correctAnswer: 1 }
                ]
            }
        ]
    },
    {
        title: "Mini Projects — Collections", order: 2,
        nodes: [
            {
                title: "Project Guide: Interactive Inventory", type: "lesson", order: 1,
                content: `## Project Guide: Interactive Inventory System\n\n### Introduction\nMost role-playing games feature an inventory system to hold the player's items. In this project, you will combine a \`while\` loop with Python Lists to build an interactive inventory menu where a player can view, add, drop, and sort items in their backpack.\n\n### Goal\nCreate a command-line interface that loops infinitely. The menu should offer 4 options: [1] View Inventory, [2] Add Item, [3] Drop Item, [4] Exit. \n\n### Requirements\n1. Initialize an empty list called \`inventory = []\`.\n2. Create a \`while True:\` loop to maintain the menu.\n3. Prompt the user for a choice.\n4. If [1], print the list, but print each item nicely formatted and numbered using a \`for\` loop.\n5. If [2], ask the user the name of the item and \`.append()\` it to the list.\n6. If [3], ask for an item name. Check if it is in the list first, and if so, \`.remove()\` it. If not, print an error.\n7. Handle the Exit command gracefully.\n\n### Step-by-Step Guide\n\n**Step 1: The Main Loop Structure**\nCreate your list at the top. \`inventory = []\`. Then open a \`while True:\` loop. Inside, \`print()\` a distinct text menu showing the options. Use \`choice = input("Select an option: ")\` to grab the user's decision.\n\n**Step 2: Viewing the Inventory**\nIf the user selects "1", check if the list is empty by doing \`if len(inventory) == 0:\`. Print "Inventory empty!" if true. \nOtherwise, loop through it. The best way to print numbered lists is using the \`enumerate()\` function. \n\`\`\`python\nfor index, item in enumerate(inventory, start=1):\n    print(f"{index}. {item}")\n\`\`\`\n\n**Step 3: Adding Items**\nIf the choice is "2", ask \`new_item = input("What did you find? ")\`. Then, call \`inventory.append(new_item)\`. Print a confirmation like "Added to backpack!".\n\n**Step 4: Removing Items Safely**\nIf the choice is "3", ask \`drop = input("Item to drop: ")\`. The \`.remove()\` method completely crashes your program if the item doesn't exist. You must guard it! \n\`\`\`python\nif drop in inventory:\n    inventory.remove(drop)\n    print(f"Dropped {drop}.")\nelse:\n    print(f"You don't have a {drop}!")\n\`\`\`\n\n### Challenge Yourself\n- If the user tries to add a 6th item, block them with a message: "Backpack is full!"\n- Add a 5th menu option to Sort the inventory alphabetically. Give it a try using \`inventory.sort()\`!`
            },
            {
                title: "Project Guide: Word Frequency Counter", type: "lesson", order: 2,
                content: `## Project Guide: Word Frequency Counter\n\n### Introduction\nData Scientists frequently parse massive amounts of text to determine trends in language. Do politicians use the word "economy" more than "security"? How do you count words quickly? In this project, you will build an analytics script that reads a sentence and counts exactly how many times each distinct word appears using Dictionaries.\n\n### Goal\nTake a sentence from the user. Split that sentence into individual words. Use a dictionary where the keys are the unique words, and the values are the numerical count of how many times that exact word appeared.\n\n### Requirements\n1. Prompt the user for a long string of text.\n2. Convert the entire string to lowercase so capitalization doesn't cause duplicate keys (e.g., "The" vs "the").\n3. Split the string into a list of words.\n4. Initialize an empty dictionary.\n5. Iterate through the word list. If the dictionary does not contain the word yet, add the key with a value of 1. If it already exists, increment the value by 1.\n6. Print the results nicely using a loop.\n\n### Step-by-Step Guide\n\n**Step 1: Data Preparation**\nAsk the user: \`text = input("Enter a paragraph: ")\`. \nStandardize the data: \`text = text.lower()\`. \nNow, convert it to a list using Python's built-in string splitting function: \`word_list = text.split()\`. This breaks the massive string into individual strings at every space character.\n\n**Step 2: The Dictionary Loop**\nCreate an empty dictionary: \`word_counts = {}\`.\nLaunch a loop: \`for word in word_list:\`.\nInside the loop, the logic goes like this: Does \`word\` already exist as a key in \`word_counts\`? \n\`\`\`python\nif word in word_counts:\n    word_counts[word] += 1  # Increment the existing score\nelse:\n    word_counts[word] = 1   # Discovering the word for the first time\n\`\`\`\n\n**Step 3: The Output Report**\nOnce the loop is finished, your dictionary holds all the answers! But printing a raw dictionary looks ugly. \nBuild an output loop. You should extract two things from the dictionary simultaneously: the word (the key) and the count (the value).\n\`\`\`python\nprint("--- Analysis Report ---")\nfor word, count in word_counts.items():\n    print(f"{word}: {count} times")\n\`\`\`\n\n### Challenge Yourself\n- Punctuation ruins word counts (e.g., "hello" and "hello," are treated as two different words). Replace all commas and periods with blank spaces \`""\` before splitting the text!\n- Sort the dictionary output so that the most frequently used words print at the very top of the list.`
            }
        ]
    }
]
  },

{
    title: "File Handling and Exceptions", order: 7,
        chapters: [
            {
                title: "Interacting with the File System", order: 1,
                nodes: [
                    {
                        title: "Reading and Writing Text Files", type: "lesson", order: 1,
                        content: `## Reading and Writing Text Files\n\n### The Need for Persistence\nWhen a Python program finishes running, every variable is destroyed. All your high scores, user preferences, and analytics vanish. To make data survive past the lifetime of the program execution, we must implement **persistence**. The simplest form of persistence is saving data to a file on your hard drive.\n\n### Using the \`open()\` Function\nPython provides a built-in \`open()\` function to interface with the operating system's file manager. \n\nThe \`open()\` function takes two primary arguments:\n1. The name of the file.\n2. The "Mode" (\`r\` for read, \`w\` for write, \`a\` for append).\n\n\`\`\`python\n# Writing to a file (WARNING: 'w' completely overwrites existing files)\nfile = open("greetings.txt", "w")\nfile.write("Hello, World!\\n")\nfile.write("Welcome to File I/O.\\n")\nfile.close() # Critical! Always close the file.\n\`\`\`\n\n### The Problem with \`file.close()\`\nLeaving a file open consumes system resources, locks the file from other applications, and can cause data corruption if the program crashes before the \`close()\` command executes. Because programmers frequently forget to close files, Python introduced a better way: The **With Statement** (Context Manager).\n\n### The With Statement\nThe \`with\` statement automatically manages the file operation. The moment the indented block of code concludes, Python safely closes the file immediately, even if the program crashed midway through!\n\n\`\`\`python\n# Safely writing to a file\nwith open("diary.txt", "w") as file:\n    file.write("Today I learned about file handling.\\n")\n\n# Safely appending to a file (perserving existing data)\nwith open("diary.txt", "a") as file:\n    file.write("I also learned about the With statement.\\n")\n\n# Safely reading from a file\nwith open("diary.txt", "r") as file:\n    content = file.read() # Reads the entire file as one giant string\n    print(content)\n\`\`\`\n\n### Reading Line by Line\nIf a file has two million lines, loading it entirely into RAM via \`.read()\` will crash your computer. Instead, you can loop over the file object, which loads the text memory efficiently, one line at a time.\n\`\`\`python\nwith open("massive_log.txt", "r") as log_file:\n    for current_line in log_file:\n        print(current_line.strip()) # strip() removes invisible trailing newlines\n\`\`\``
                    },
                    {
                        title: "Writing and Parsing CSV and JSON", type: "lesson", order: 2,
                        content: `## Writing and Parsing CSV and JSON\n\n### Structured Data\nWriting raw text strings to a file is okay for a diary, but terrible for actual software. Imagine saving a list of employees to a text file. You need structure so a program can open it later, reliably convert the text back into variables, and understand what the data means. To solve this, developers standardized formats like CSV and JSON.\n\n### Working with CSV\nCSV stands for Comma-Separated Values. It resembles a spreadsheet. Each line is a row, and each column is separated by a comma (or sometimes a semicolon). Python has a built-in module for handling this format safely, automatically handling edge cases like commas appearing inside actual string data.\n\n\`\`\`python\nimport csv\n\n# Writing a CSV\nemployees = [\n    ["Alice", "Engineering", 75000],\n    ["Bob", "Marketing", 62000]\n]\n\nwith open('staff.csv', 'w', newline='') as file:\n    writer = csv.writer(file)\n    writer.writerow(["Name", "Department", "Salary"]) # Write the header\n    writer.writerows(employees) # Write all the data\n\n# Reading a CSV\nwith open('staff.csv', 'r') as file:\n    reader = csv.reader(file)\n    for row in reader:\n        print(f"Name: {row[0]}, Dept: {row[1]}")\n\`\`\`\n\n### Working with JSON\nJSON (JavaScript Object Notation) is arguably the most vital data format in the modern world. Almost all internet APIs use JSON. It perfectly maps to Python Dictionaries and Lists, meaning you can save complex, nested Python data structures directly to a file, and reload them perfectly intact.\n\n\`\`\`python\nimport json\n\nuser_data = {\n    "username": "coder123",\n    "preferences": {\n        "theme": "dark",\n        "notifications": False\n    },\n    "high_scores": [150, 142, 108]\n}\n\n# json.dump() converts a Python dictionary and writes it to a file\nwith open('settings.json', 'w') as file:\n    json.dump(user_data, file, indent=4) # indent makes it human-readable\n\n# json.load() reads the file back into a full Python dictionary\nwith open('settings.json', 'r') as file:\n    loaded_data = json.load(file)\n    print(loaded_data["username"]) # "coder123"\n\`\`\`\nUsing the \`json\` module is the gold standard for saving configuration files or robust save-game states.`
                    },
                    {
                        title: "Handling Errors with Try/Except", type: "lesson", order: 3,
                        content: `## Handling Errors with Try/Except\n\n### The Inevitability of Failure\nPrograms will crash. It is an unavoidable fact of software development. No matter how perfect your logic is, things outside your control will go wrong:\n- A user types the word "five" when your code attempts to cast \`int(input())\`.\n- Your program attempts to open \`high_scores.txt\`, but the user deleted the file.\n- Your script attempts to download data, but the Wi-Fi disconnects.\n\nWhen a Python script hits a fatal error (an **Exception**), it completely aborts execution and prints an angry traceback error. This is terrible user experience. \n\n### The Try/Except Block\nPython allows you to anticipate crashes and handle them gracefully using \`try\` and \`except\`. \nYou place the risky code inside the \`try\` block. If that code succeeds, the program continues normally. If it fails, Python instantly aborts the \`try\` block and jumps into the \`except\` block instead of crashing the program.\n\n\`\`\`python\ntry:\n    # Risky code: Asking for a number\n    age = int(input("Enter your age: "))\n    print(f"You will be {age + 10} in a decade.")\nexcept ValueError:\n    # This executes ONLY if the specific ValueError (bad type casting) occurred\n    print("Error: You did not type a valid number! Please use digits.")\n\`\`\`\n\n### Catching Specific Exceptions\nYou should never use a "bare except" (just the word \`except:\` with nothing after it). Doing so blinds your program. It will catch the error you expected, but it will also silently catch and hide bugs and typos that you *didn't* expect, making debugging impossible. Always target the specific error name!\n\n\`\`\`python\ntry:\n    with open("config.json", "r") as file:\n        data = file.read()\n    # Assume a variable named 'result' requires division\n    result = 100 / int(data)\n\nexcept FileNotFoundError:\n    print("Configuration file missing. Creating defaults.")\nexcept ValueError:\n    print("The file contained text instead of a number.")\nexcept ZeroDivisionError:\n    print("Cannot divide by zero!")\n\`\`\`\n\n### Features: Finally\nYou can attach a \`finally\` block to the end. The \`finally\` block executes absolutely no matter what happens—whether the \`try\` succeeded, failed, or even returned out of a function early. This is vital for critical cleanup operations, such as forcefully closing a network connection or clearing sensitive data from memory.\n\`\`\`python\ntry:\n    process_data()\nexcept Exception:\n    print("Failure.")\nfinally:\n    print("Cleanup sequence initiated.")\n\`\`\``
                    },
                    {
                        title: "File Handling Quiz", type: "quiz", order: 4,
                        questions: [
                            { question: "Why is the `with` statement preferred when opening files?", options: ["It restricts access to authorized users", "It encrypts the file automatically", "It guarantees the file is safely closed when the block ends, preventing corruption", "It runs significantly faster"], correctAnswer: 2 },
                            { question: "What happens if you open a file in the 'w' (write) mode when a file with that name already exists?", options: ["An exception is thrown", "It appends data to the end of the file", "It completely overwrites the file, destroying all previous data", "It renames the old file to a backup"], correctAnswer: 2 },
                            { question: "Which data format maps perfectly cleanly to Python Dictionaries allowing deep serialization?", options: ["CSV", "XML", "JSON", "TXT"], correctAnswer: 2 },
                            { question: "What happens if code crashes inside a `try` block?", options: ["The program aborts and prints a traceback", "Python skips the rest of the try block and jumps immediately into the except block", "Python skips the current line and forces execution to the next line inside the try block", "The computer requires a restart"], correctAnswer: 1 }
                        ]
                    }
                ]
            },
            {
                title: "Mini Projects — Files & Exceptions", order: 2,
                nodes: [
                    {
                        title: "Project Guide: Log File Analyzer", type: "lesson", order: 1,
                        content: `## Project Guide: Log File Analyzer\n\n### Introduction\nEvery modern server constantly generates log files tracking network access and system errors. Often, administrators write Python scripts to parse these logs and extract the critical errors so they don't have to read thousands of lines manually. In this project, you will build exactly that.\n\n### Goal\nYou will write a script that opens a text file consisting of mocked server logs. Your program will loop through the file, extract ONLY the lines that say "ERROR" or "CRITICAL", and write those specific lines into a brand new standalone file called \`error_report.txt\`.\n\n### Requirements\n1. Use Python to first create a massive mock log file to analyze by looping and writing dummy strings.\n2. Utilize a \`try/except\` block wrapped around your file opening statement. Attempt to catch the \`FileNotFoundError\` specifically.\n3. Open the original log file in "Read" mode, and open your output \`error_report.txt\` file simultaneously in "Write" mode using a comma-separated \`with\` statement.\n4. Read through the log line by line.\n5. If the substring "ERROR" exists within the line, \`.write()\` that line into the output file.\n6. Print a console message stating how many errors were successfully ported over.\n\n### Step-by-Step Guide\n\n**Step 1: Set the Stage**\nBefore writing the real tool, create the dummy data. \nWrite a small block to create \`server.log\`. Write 5 lines into it using a \`with open() as f:\`. Make sure two of those lines contain the word "ERROR".\n\n**Step 2: Dual File Handling**\nYou can open multiple files at the exact same time! This prevents you from having to hold the entire log in memory.\n\`\`\`python\nwith open("server.log", "r") as source_file, open("error_report.txt", "w") as destination_file:\n\`\`\`\n\n**Step 3: The Filter Loop**\nUnderneath the \`with\` statement definition, create an \`error_tally\` variable set to 0. \nUse the \`for line in source_file:\` syntax to read the file line by line safely. \nFor each line, build an \`if "ERROR" in line:\` check. If it proves True, call \`destination_file.write(line)\`, and increment your tracker variable!\n\n**Step 4: Error Handling Check**\nBefore submitting the code, move the entire reading logic into a \`try\` block. Underneath it, construct the \`except FileNotFoundError:\` block simply printing a clean error message. Delete the \`server.log\` file manually from your folder, run the script, and ensure the exception prints cleanly without crashing your whole terminal.\n\n### Challenge Yourself\n- Expand the requirement so that the script catches both "ERROR" and "WARNING" strings, and appends them to their respective output files dynamically.`
          },
            {
                title: "Project Guide: JSON Contact Book", type: "lesson", order: 2,
                content: `## Project Guide: JSON Contact Book\n\n### Introduction\nA Python dictionary is powerful, but when the program stops, the dictionary drops from RAM. In this capstone project for this section, you will use the \`json\` module to ensure changes made to a dictionary are saved persistently to disk, creating your first true database-backed application.\n\n### Goal\nYou will write an interactive application that acts as an address book. The app must load its state from a JSON file upon booting. The user then has menu options to view contacts, add a new contact, or delete a contact. Any changes made must be instantly saved back into the JSON file.\n\n### Requirements\n1. Import the \`json\` and \`os\` modules.\n2. In a loop, attempt to open \`contacts.json\`. If the file doesn't exist yet, catch the \`FileNotFoundError\`, and initialize an empty dictionary variable \`contacts = {}\`.\n3. Display an interactive terminal menu using a \`while True:\` loop.\n4. Option 1: View Contacts (Pretty print the dictionary).\n5. Option 2: Add Contact. Ask for a name (the Key) and a phone number (the Value). Add them to the dictionary. IMMEDIATELY call a save function leveraging \`json.dump()\`.\n6. Option 3: Exit gracefully.\n\n### Step-by-Step Guide\n\n**Step 1: The Loader**\nWrite a try/except block. Try to do \`with open("contacts.json", "r") as file:\` and call \`contacts = json.load(file)\`. In the \`except FileNotFoundError:\` block, assign \`contacts = {}\`.\n\n**Step 2: The Saving Function**\nBecause you will need to save the file repeatedly in different menu choices, define a dedicated helper function: \`def save_data():\`. Inside the function, open the file in \`'w'\` mode and execute \`json.dump(contacts, file, indent=4)\`.\n\n**Step 3: The CLI Menu**\nSet up your standard text menu loop. Ask for user choice.\n\n**Step 4: Displaying Logic**\nIf the user chooses Option 1, use a \`for name, phone in contacts.items():\` loop to print each person. If the dictionary is totally empty, print "Your contact book is empty!"\n\n**Step 5: Appending Logic**\nIf the user chooses Option 2, ask for the inputs. Add it to the dictionary: \`contacts[name_input] = phone_input\`. Critically, you must now call your customized \`save_data()\` function so that the file on the hard drive reflects the update!\n\n### Challenge Yourself\n- Implement validation where you block the user from saving a phone number unless the string they typed is made completely out of integers. Hint: use \`phone.isdigit()\`.\n- Build a deletion option. Check if the dictionary key exists utilizing \`if target in contacts: \`, and if it does, erase it with the \`del contacts[target]\` expression, then save the database!`
}
        ]
      }
    ]
  }
];
