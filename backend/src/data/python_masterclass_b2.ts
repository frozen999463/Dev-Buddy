// Python Masterclass — Part B2 (Sections 8-9)
export const pythonMasterclassPartB2 = [
    {
        title: "Object-Oriented Programming (OOP)", order: 8,
        chapters: [
            {
                title: "Classes and Objects", order: 1,
                nodes: [
                    {
                        title: "Introduction to OOP and Classes", type: "lesson", order: 1,
                        content: `## Introduction to Object-Oriented Programming\n\n### The Problem with Variables\nSo far, we have been doing "Procedural Programming." We create a few variables, write some functions to manipulate them, and execute from top to bottom. But what happens when we model something complex? \nImagine building a game. You need a hero. The hero has a name, health, armor, and a weapon. You could create four variables:\n\`hero_name = "Arthur"\`, \`hero_health = 100\`, \`hero_armor = 50\`, \`hero_weapon = "Sword"\`.\nNow add an enemy. Four more variables. Add ten enemies. Forty variables. It becomes an impossible web of disconnected strings and integers!\n\n### Think in Objects\nObject-Oriented Programming (OOP) solves this by letting you create your own custom data types called **Objects**. An object binds data (variables) and behavior (functions) together into a single, cohesive unit. Instead of loose variables, you have one \`Player\` object that knows its own name, tracks its own health, and can swing its own weapon.\n\nYou interact with the world through objects. A \`Car\` object has a \`speed\` attribute and a \`drive()\` method. A \`BankAccount\` object has a \`balance\` attribute and a \`withdraw()\` method.\n\n### Defining a Class (The Blueprint)\nA **Class** is the blueprint or the DNA of an object. It defines what attributes and methods the objects will have, but it isn't the object itself. Think of a class as the architectural drawing of a house. You can't live in the drawing. But from that one drawing, you can build ten identical houses. Those actual houses are the **Objects** (also called Instances).\n\n\`\`\`python\n# Creating the Blueprint using CamelCase naming convention\nclass Dog:\n    # Methods are functions that belong to a class\n    def bark(self):\n        print("Woof!")\n\n# Building the actual Objects from the Blueprint\nmy_dog = Dog() \nmy_neighbor_dog = Dog()\n\n# Utilizing the object\nmy_dog.bark()\n\`\`\`\nEverything in Python is an object behind the scenes. When you create a list \`[1, 2, 3]\`, Python uses the built-in \`List\` blueprint to construct a list object!`
          },
            {
                title: "Attributes, Methods, and __init__", type: "lesson", order: 2,
                content: `## Attributes, Methods, and __init__\n\n### The Magcal __init__ Method\nWhile building a \`Dog\` object that can bark is cool, it isn't very useful unless that dog has uniquely identifying data. My dog is named "Rex" and is 3 years old. Your dog is named "Spot" and is 5 years old. We need to initialize our objects with this data the exact moment they are born.\n\nPython provides a strict, built-in constructor method named \`__init__\` (short for initialize) with double underscores on both sides. This method is automatically executed the millisecond a new object is instantiated.\n\n\`\`\`python\nclass Dog:\n    # The constructor method\n    def __init__(self, given_name, given_age):\n        self.name = given_name  # Creating an attribute\n        self.age = given_age    # Creating an attribute\n\n# Instantiating objects with unique data\nmy_dog = Dog("Rex", 3)\nyour_dog = Dog("Spot", 5)\n\nprint(my_dog.name)  # Outputs: Rex\nprint(your_dog.name) # Outputs: Spot\n\`\`\`\n\n### The Mystery of 'self'\nYou noticed the word \`self\`. Inside a class blueprint, \`self\` represents the specific, actual object being created or manipulated right now. When you do \`self.name = given_name\`, you are attaching a sticky note called \`name\` directly to the object itself.\n\nWhenever you define a method inside a Python class, the very first parameter MUST be \`self\`. Python automatically passes the object reference into that spot so the method knows whose data it is modifying.\n\n\`\`\`python\nclass BankAccount:\n    def __init__(self, owner, starting_balance):\n        self.owner = owner\n        self.balance = starting_balance\n        \n    def deposit(self, amount):\n        self.balance += amount # Accessing its own data via self\n        print(f"Deposited {amount}. New total for {self.owner} is {self.balance}")\n\naccount1 = BankAccount("Alice", 1000)\naccount1.deposit(500) # Python secretly converts this to BankAccount.deposit(account1, 500)\n\`\`\``
            },
            {
                title: "Inheritance and Polymorphism", type: "lesson", order: 3,
                content: `## Inheritance and Polymorphism\n\n### The Concept of Inheritance\nProgrammers are incredibly lazy, which makes us highly efficient. If you spend hours building a \`Vehicle\` class that has an engine, wheels, and a \`drive()\` method, and tomorrow your boss asks you to build a \`Motorcycle\` class, you shouldn't have to rewrite everything from scratch.\n\n**Inheritance** allows a new "Child" class to inherit absolutely all attributes and methods from an existing "Parent" class.\n\n\`\`\`python\nclass Animal: # The Parent\n    def __init__(self, name):\n        self.name = name\n        \n    def eat(self):\n        print(f"{self.name} is eating food.")\n\n# The Child inherits from Animal by putting it in parentheses\nclass Cat(Animal): \n    def meow(self):\n        print(f"{self.name} says Meow!")\n\n# Our cat gets both the Cat methods AND the Animal methods!\nkitty = Cat("Whiskers")\nkitty.meow()\nkitty.eat() \n\`\`\`\n\n### Overriding Methods\nWhat if the Child inherits a method from the Parent, but that method isn't quite right it? A generic \`Animal\` might have a \`sleep()\` method that prints "Sleeping on the ground". But a \`Bat\` sleeps upside down! \n\nYou can override the method simply by defining a method with the exact same name in the Child class. Python will always use the Child's version first.\n\n\`\`\`python\nclass Bat(Animal):\n    def sleep(self): # Overrides the parent Animal sleep method\n        print(f"{self.name} is sleeping upside down.")\n\`\`\`\n\n### Super()\nSometimes, you want to override the Parent, but you still need the Parent to do its original job first. You can use the \`super()\` keyword. This forces Python to look up at the Parent class and execute its logic.\nThis is most commonly used in the \`__init__\` method, where the Child wants to set up some new variables, but still needs the Parent to set up the foundation.\n\n\`\`\`python\nclass Hero(Animal):\n    def __init__(self, name, power):\n        super().__init__(name) # Hand the name up to Animal to assign\n        self.power = power     # Assign the new Child-only attribute\n\`\`\``
            },
            {
                title: "OOP Quiz", type: "quiz", order: 4,
                questions: [
                    { question: "What is the difference between a Class and an Object?", options: ["A Class is an instance of an Object", "A Class is the blueprint, while an Object is the actual item built from that blueprint", "They are exactly the same thing", "An Object holds functions, a Class holds variables"], correctAnswer: 1 },
                    { question: "What is the purpose of the `__init__` method?", options: ["It deletes the object from RAM", "It serves as the constructor to set up initial data the moment an object is created", "It halts the program and initializes debugging mode", "It encrypts the class code"], correctAnswer: 1 },
                    { question: "What does the `self` keyword represent inside a class method?", options: ["The parent class", "A global variable", "The specific object instance that called the method", "The python interpreter"], correctAnswer: 2 },
                    { question: "What does `super().__init__()` do?", options: ["It turns the object into an admin", "It deletes the parent attributes", "It calls the constructor of the Parent class so the Child inherits foundational setup logic", "It prevents the child from having attributes"], correctAnswer: 2 }
                ]
            }
        ]
    },
    {
        title: "Mini Projects — Object-Oriented", order: 2,
        nodes: [
            {
                title: "Project Guide: RPG Character Simulator", type: "lesson", order: 1,
                content: `## Project Guide: RPG Character Simulator\n\n### Introduction\nObject-Oriented Programming (OOP) is heavily used in video game development because games consist of distinct entities interacting. In this project, you will create a \`Character\` class modeling an RPG hero, and then instantiate several heroes to battle each other.\n\n### Goal\nBuild a \`Character\` class containing attributes like name, health, and attack power. Implement custom methods that allow the character to take damage, attack an enemy, and drink a healing potion. Test the class by staging a mock fight in the terminal between two instantiated objects.\n\n### Requirements\n1. Define \`class Character:\` with an \`__init__\` method that takes \`name\`, \`health\`, and \`attack_power\`.\n2. Create a \`take_damage(self, amount)\` method that deducts health and uses an \`if\` statement to check if health fell below zero (printing "X has died!" if so).\n3. Create an \`attack(self, target)\` method. Crucially, the target argument passed in will be ANOTHER instance of the Character class! This method must invoke \`target.take_damage(self.attack_power)\`.\n4. Instantiate two characters: e.g., \`hero = Character("Arthur", 100, 20)\` and \`ork = Character("Thrall", 80, 25)\`.\n5. Call their attack methods on each other.\n\n### Step-by-Step Guide\n\n**Step 1: The Blueprint**\nDefine \`class Character:\`. Inside, define \`def __init__(self, name, hp, attack):\`. Assign \`self.name = name\`, \`self.hp = hp\`, etc.\n\n**Step 2: Receiving Damage**\nDefine \`def take_damage(self, damage):\`. \nCalculate \`self.hp -= damage\`. \nPrint a message: \`print(f"{self.name} took {damage} damage! HP is now {self.hp}")\`. \nUse an if-statement to handle death: \`if self.hp <= 0: print(f"{self.name} has been defeated.")\`\n\n**Step 3: Delivering Damage**\nDefine \`def attack(self, target_character):\`. \nPrint an action statement: \`print(f"{self.name} swings at {target_character.name}!")\`. \nExecute the damage calculation by calling the target's method: \`target_character.take_damage(self.attack_power)\`.\n\n**Step 4: The Showdown**\nOutside the class, initialize your two fighters. \nCall \`hero.attack(ork)\`. Then call \`ork.attack(hero)\`. Run your script and watch the text-based battle unfold!\n\n### Challenge Yourself\n- Create a \`drink_potion(self)\` method that restores 30 HP but caps the maximum health back at 100 using a mathematical \`min()\` or an \`if\` statement. \n- Use a \`while\` loop outside the class to make the two heroes attack each other automatically until one of their \`.hp\` values drops to 0 or below!`
          },
{
    title: "Project Guide: Bank Account System", type: "lesson", order: 2,
        content: `## Project Guide: Bank Account System\n\n### Introduction\nIn financial software, extreme control over data is necessary. OOP helps encapsulate sensitive data and restrict its manipulation. You will design a \`BankAccount\` class that tracks a user's balance and enforces strict logic regarding withdrawals.\n\n### Goal\nCreate a \`BankAccount\` class that strictly prohibits negative balances. Implement methods for safe deposits, safe withdrawals, and displaying statements. Demonstrate its effectiveness by purposely trying to withdraw more money than the account holds.\n\n### Requirements\n1. The \`BankAccount\` class must have an \`__init__\` receiving an \`account_holder_name\` and an initial \`balance\`.\n2. The class needs a \`deposit(self, amount)\` method that accepts only positive numbers.\n3. The class requires a \`withdraw(self, amount)\` method that checks if the user requested more money than \`self.balance\`. If so, it must print an error and refuse the transaction.\n4. A \`display_statement(self)\` method summarizing the account's state.\n5. Instantiate a bank account, perform several valid operations, and one intentionally invalid operation to test your logic.\n\n### Step-by-Step Guide\n\n**Step 1: Initializing**\nDefine the class and constructor. \n\`def __init__(self, owner, starting_balance = 0.0):\` \n(Notice the default argument? This allows users to open an empty account!). Tie them to \`self.owner\` and \`self.balance\`.\n\n**Step 2: Depositing**\nDefine \`def deposit(self, amount):\`. \nValidate the transaction to prevent hacking: \`if amount <= 0: print("Cannot deposit negative money!")\`. Else, \`self.balance += amount\`, then print a success message reflecting the new total.\n\n**Step 3: Withdrawing**\nDefine \`def withdraw(self, amount):\`. \nFirst, test if the requested amount is larger than \`self.balance\`. \`if amount > self.balance:\`. If True, reject it explicitly with a print statement: "Insufficient funds!". \nElse, \`self.balance -= amount\` and print the success receipt.\n\n**Step 4: The Interface**\nCreate the \`get_balance(self)\` method to return an f-string summarizing the owner's status. "Account [Name] has balance: $[Balance]".\n\n**Step 5: Testing**\nAt the bottom of your script, initialize \`my_bank = BankAccount("John Doe", 50)\`. \nTry \`my_bank.deposit(50)\`. \nTry \`my_bank.withdraw(20)\`. \nTry \`my_bank.withdraw(1000)\` (which should fail!).\n\n### Challenge Yourself\n- Expand the system using **Inheritance**! Create a \`SavingsAccount\` child class that inherits from \`BankAccount\`.\n- Override the child's \`__init__\` using \`super()\` to add an \`interest_rate\` attribute (e.g., 0.05).\n- Give the child an \`apply_interest(self)\` method that multiplies the balance by the rate and deposits it!`
}
        ]
      }
    ]
  },

{
    title: "Software Engineering Principles", order: 9,
        chapters: [
            {
                title: "Writing Clean Python", order: 1,
                nodes: [
                    {
                        title: "PEP 8 and Readable Code", type: "lesson", order: 1,
                        content: `## PEP 8 and Readable Code\n\n### The Importance of Readability\nCode is read far more often than it is written. You might spend two hours writing a complex algorithm, but over the next five years, twenty different developers will read it. If they can't understand it, they will replace it or break it. \n\nGuido van Rossum (the creator of Python) famously said: "Code is read much more often than it is written." Python was built entirely around this philosophy. But to ensure consistency globally, the Python core developers published **PEP 8** (Python Enhancement Proposal #8), the official style guide for Python.\n\n### Key Rules of PEP 8\n\n1. **Indentation and Spacing:** Always use 4 spaces per indentation level. Do not use tabs, as tabs render differently across different operating systems and text editors, leading to nightmare formatting bugs.\n2. **Maximum Line Length:** Limit all lines to a maximum of 79 characters. This accommodates developers viewing files side-by-side on small monitors.\n3. **Blank Lines:** Surround top-level functions and class definitions with two blank lines. Use single blank lines to visually separate logical sections inside a massive function.\n4. **Imports:** Imports should exclusively live at the absolute top of the file, not scattered throughout. Do not put multiple imports on a single line (\`import sys, os\` is frowned upon; put them on separate lines).\n\n### Naming Conventions\nIf you adhere to these naming rules, any senior developer can instantly recognize what your variables represent.\n- **Functions & Variables:** Must be \`snake_case\` (all lowercase, underscores separating words).\n- **Classes:** Must be \`PascalCase\` (no underscores, every word capitalized: \`class UserAccount:\`).\n- **Constants:** Must be \`UPPER_SNAKE_CASE\` (\`TOTAL_MAX_RETRIES = 5\`).\n\n### Whitespace in Expressions\nDo not use a huge amount of spaces around your equals signs, and always put exactly one space around commas and operators.\n\n\`\`\`python\n# BAD\nx             = 1\ny_list = [ 1 , 2 ,3]\ncalculated_amount=x*y_list[0]\n\n# PEP 8 APPROVED\nx = 1\ny_list = [1, 2, 3]\ncalculated_amount = x * y_list[0]\n\`\`\`\nAdhering to PEP 8 from day one separates amateurs from professionals.`
                    },
                    {
                        title: "Modules, Packages, and Imports", type: "lesson", order: 2,
                        content: `## Modules, Packages, and Imports\n\n### Breaking Out of the Single File Concept\nAs your Python project reaches thousands of lines, maintaining a single giant \`main.py\` file becomes impossible. Software engineering relies on modularity—breaking code into logical "Modules." A module in Python is simply a \`.py\` file filled with variables, functions, or classes.\n\nImagine you wrote a brilliant function that calculates mortgage rates. If you place it in a separate file named \`finance.py\`, you can now reuse that function in ten different projects without ever copy-pasting it.\n\n### The Import Statement\nTo gain access to the logic inside a module, you use the \`import\` keyword. Let's assume you have two files sitting in the same folder: \`app.py\` and \`finance.py\`.\n\nIn \`finance.py\`:\n\`\`\`python\ndef calculate_tax(amount):\n    return amount * 0.2\n\`\`\`\n\nIn \`app.py\`:\n\`\`\`python\nimport finance\n\n# You must prefix the function with the module name\n# This tells Python EXACTLY where to find it.\nresult = finance.calculate_tax(100)\nprint(result)\n\`\`\`\n\n### Selective Importing\nIf \`finance.py\` possesses 500 different functions, it is inefficient and messy to prefix every command with \`finance.\`. Instead, you can import specific functions directly into your file's namespace using the \`from ... import ...\` syntax.\n\n\`\`\`python\nfrom finance import calculate_tax, calculate_interest\n\n# Now you skip the prefix entirely!\ntotal = calculate_tax(500) \n\`\`\`\n\n### The Standard Library and Packages\nYou don't just import your own files. Python installs with a massive "Standard Library" of modules you can import immediately, like \`math\`, \`random\`, \`datetime\`, and \`json\`.\n\nWhen a developer creates a folder containing multiple Python modules organized together, it is called a **Package**. PyPI (The Python Package Index) holds hundreds of thousands of community-built packages. By running \`pip install requests\` in your terminal, you download a highly professional web-scraping package built by another engineer. You then write \`import requests\` at the top of your file to inject their thousands of hours of work instantly into your application. This is the true power of module resolution.`
                    },
                    {
                        title: "The __main__ Guard and Script Execution", type: "lesson", order: 3,
                        content: `## The \`__main__\` Guard and Script Execution\n\n### The Import Execution Trap\nThere is a critical quirk to how Python processes files that every advanced engineer must understand. **When you \`import\` a Python module, Python literally executes and evaluates every single line in that file from top to bottom!**\n\nIf you have a file \`calculator.py\`: \n\`\`\`python\ndef add(a, b):\n    return a + b\n\n# Test code testing the function\nprint("Test result:", add(2, 2))\n\`\`\`\nNow imagine you are writing a massive server application in \`server.py\` and you write \`import calculator\`. Instantly, upon running your server, the console will print "Test result: 4". Python ran your test code simply because you imported the file! This is disastrous. We only want Python to evaluate the function definitions, not run random code at the bottom.\n\n### The Fix: \`if __name__ == "__main__":\`\nPython assigns a secret, built-in string variable named \`__name__\` to every single file it executes.\n\n1. If a file is executed directly from the terminal (e.g., \`python3 calculator.py\`), Python forcefully assigns the \`__name__\` variable inside that specific file the value of the string \`"__main__"\`.\n2. If a file is imported by someone else (e.g., \`import calculator\`), Python sets the \`__name__\` variable to the file's literal name (e.g., \`"calculator"\`).\n\nWe utilize this secret variable to build a security gate called the Main Guard. We place all our executable action code inside an \`if\` block at the absolute bottom of the file.\n\n\`\`\`python\ndef add(a, b):\n    return a + b\n\n# --- The Main Guard ---\nif __name__ == "__main__":\n    # This code ONLY fires if the user ran THIS file directly.\n    # If another file imports this module, this code is 100% ignored!\n    print("Executing standalone calculator tests...")\n    print(add(5, 5))\n\`\`\`\nCheck any professional, open-source repository on GitHub. Look at the bottom of the Python scripts. You will see \`if __name__ == "__main__":\` acting as the guardian protecting against accidental execution on import.`
                    },
                    {
                        title: "Software Engineering Quiz", type: "quiz", order: 4,
                        questions: [
                            { question: "According to PEP 8, what is the standard tool used for code block indentation?", options: ["2 spaces", "4 spaces", "One horizontal tab", "Indents are not required in Python"], correctAnswer: 1 },
                            { question: "If you want to use the `download_file()` function from the `network.py` module without typing `network.download_file()`, what import statement do you use?", options: ["import network as all", "from network import download_file", "import download_file from network", "fetch network"], correctAnswer: 1 },
                            { question: "Why do programmers place code inside the block `if __name__ == '__main__':`?", options: ["To prevent variables from being garbage collected", "To guarantee that code only runs when the script is executed directly, preventing it from incorrectly running when the file is imported by another module", "To make it a global constant accessible across the CPU", "Because it makes the function run 50% faster"], correctAnswer: 1 },
                            { question: "What happens when you execute `import string_utils` inside your file?", options: ["Python scans the code for viruses", "Python links the C binary", "Python executes the entire string_utils.py file from top to bottom immediately", "Python encrypts the file"], correctAnswer: 2 }
                        ]
                    }
                ]
            },
            {
                title: "Mini Projects — Software Engineering", order: 2,
                nodes: [
                    {
                        title: "Project Guide: Modular Math Library", type: "lesson", order: 1,
                        content: `## Project Guide: Modular Math Library\n\n### Introduction\nA hallmark of a seasoned engineer is writing clean, separated modules instead of cramming everything into one file. In this project, you will build a mathematical software suite split across two separate \`.py\` files to practice importing and utilizing the \`__main__\` guard effectively.\n\n### Goal\nCreate a \`math_lib.py\` file comprising four calculation functions. Create a separate \`app.py\` script that imports your mathematical library and builds an interactive terminal loop to use those functions. Prove that no accidental code execution occurs during the import.\n\n### Requirements\n1. In \`math_lib.py\`, define functions for \`add\`, \`subtract\`, \`multiply\`, and \`divide\`.\n2. In \`math_lib.py\`, write a small test block using \`print()\` that tests each function. \n3. Guard those test blocks underneath \`if __name__ == "__main__":\`.\n4. Create \`app.py\` in the exact same directory.\n5. Instruct \`app.py\` to leverage \`from math_lib import ...\` syntax.\n6. In \`app.py\`, write a simple \`while\` loop interface asking the user what math operation they desire to run.\n\n### Step-by-Step Guide\n\n**Step 1: The Library Engine**\nOpen a fresh file named \`math_lib.py\`. Define your four basic functions. \n\`def add(a, b): return a + b\`, etc. \nAt the absolute bottom, add the \`if __name__ == "__main__": \` block. Beneath it, write \`print("Running library tests:", add(2, 2))\`.\nRun \`python math_lib.py\` in your terminal to ensure the tests successfully output a 4.\n\n**Step 2: The Application Shell**\nCreate a pristine \`app.py\` file. \nWrite \`from math_lib import add, subtract, multiply, divide\` at line 1.\nRun your \`app.py\` file immediately. The terminal should be entirely blank! Even though you imported the library, the tests guarded behind \`__main__\` were perfectly suppressed.\n\n**Step 3: The CLI Controller**\nInside \`app.py\`, initialize a standard \`while True: \` loop. Formulate a text menu using \`print()\`. \nUse \`input\` to gather operation choices and target numbers. \nIf the user requests addition, call your imported \`add(num1, num2)\` function, capture the returned result into a variable, and \`print()\` it back to the user utilizing an f-string.\n\n### Challenge Yourself\n- Expand \`math_lib.py\` by engineering an advanced function: \`calculate_hypotenuse(a, b)\`. Use the Pythagorean theorem! You will have to \`import math\` solely *inside* your \`math_lib.py\` file to gain access to \`math.sqrt()\`.\n- Build validation. Modify \`divide\` in your library so that if \`b == 0\`, it intentionally \`return None\` rather than throwing a catastrophic \`ZeroDivisionError\`. Ensure your \`app.py\` checks for \`None\` and warns the user!`
},
{
    title: "Project Guide: Terminal To-Do App", type: "lesson", order: 2,
        content: `## Project Guide: Terminal To-Do Application\n\n### Introduction\nThe quintessential portfolio project for every programmer is a To-Do List application. It consolidates everything you have learned: Variables, Control Flow loops, Functions, complex List Collections, and PEP 8 structural guidelines. You will act as the software architect, planning and assembling the whole program.\n\n### Goal\nConstruct an interactive To-Do command-line application centered around a dynamic Python List. Abstract the complex list manipulation logic into clean, readable functions with appropriate docstrings, strictly separating interface mechanics from data modification.\n\n### Requirements\n1. Define an empty global \`task_list = []\`.\n2. Create an \`add_task(task_name)\` function containing the logic to append to the list.\n3. Create a \`view_tasks()\` function containing a \`for\` loop combined with \`enumerate()\` to neatly display the task_list payload.\n4. Create a \`complete_task(task_index)\` function that utilizes the \`.pop()\` method to delete an item based on its index position provided by the user.\n5. Ensure all core logic is triggered via a \`while True: \` menu interface embedded strictly inside a \`if __name__ == "__main__": \` evaluation block.\n\n### Step-by-Step Guide\n\n**Step 1: Defining the Core Functions**\nBegin your script by defining your three core functions. They should purely manipulate data, not ask for \`input()\` themselves. \n- \`def add_task(task_name): \` executes \`task_list.append(task_name)\` and prints a confirmation.\n- \`def view_tasks(): \` first tests \`if not task_list: print("List is empty")\`, otherwise it loops \`for idx, val in enumerate(task_list): print(f"{idx}: {val}")\`.\n- \`def complete_task(index): \` uses a \`try/except IndexError:\` wrapper. Inside, it calls \`finished = task_list.pop(index)\` and prints a success string containing the \`finished\` variable.\n\n**Step 2: Constructing the Controller**\nAt the terminus of the file, orchestrate the execution.\n\`\`\`python\nif __name__ == "__main__":\n    # The CLI loop lives here!\n    while True:\n        print("\\n1. Add Task  2. View Tasks  3. Complete Task  4. Disconnect")\n        choice = input("Awaiting directive: ")\n        ...\n\`\`\`\n\n**Step 3: Binding Interface to Logic**\nFill the \`if/elif\` logic inside your loop. If the user invokes choice "1", trigger \`ui_task = input("Task title: ")\` and subsequently dispatch it to your engine with \`add_task(ui_task)\`. \nIf choice "3" is requested, ask the user for the task number, forcefully cast the response to an integer using \`int()\`, and pass it dynamically into \`complete_task(user_int)\`. Ensure choice "4" initiates a loop \`break\`.\n\n### Challenge Yourself\n- Upgrade the logic. Instead of standard string lists, utilize Dictionaries! Structure your global list as \`[{ "title": "Buy Milk", "status": "Pending" }]\`.\n- Upon completing a task, instead of deleting it with \`.pop()\`, update the dictionary status key to \`"Done"\`.`
}
        ]
      }
    ]
  }
];
