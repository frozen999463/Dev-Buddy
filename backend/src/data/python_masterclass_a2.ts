// Python Masterclass — Part A2 (Sections 4-5)
export const pythonMasterclassPartA2 = [
    {
        title: "Control Flow", order: 4,
        chapters: [
            {
                title: "Decision Making and Loops", order: 1,
                nodes: [
                    {
                        title: "Making Decisions with If/Elif/Else", type: "lesson", order: 1,
                        content: `## Making Decisions with If/Elif/Else\n\n### The Need for Control Flow\nUp until now, our Python scripts have executed straight down. Line 1 runs, then Line 2, then Line 3, and then the program ends. But real software is dynamic. A video game needs to check if your character's health has dropped below zero. A banking app needs to verify if your password is correct before showing your balance. \n\nThis ability to diverge the execution path based on different conditions is called **Control Flow**, and the most fundamental tool for this is the \`if\` statement. Conditional logic allows our programs to make decisions in real-time.\n\n### How the If Statement Works\nIn Python, an \`if\` statement evaluates an expression that results in a Boolean (\`True\` or \`False\`). If the expression is \`True\`, the indented block of code directly underneath it is executed. If it is \`False\`, Python completely skips that block.\n\n\`\`\`python\nage = 20\nif age >= 18:\n    print("Welcome to the application.")\n    print("You have full access.")\n\`\`\`\nNotice the **indentation**. Python uses spaces (usually four spaces) to define a "block" of code. Everything indented under the \`if\` statement is part of what happens when the condition is met. Once you stop indenting, you are back out of the \`if\` block.\n\n### Handling Alternatives: Else and Elif\nWhat if the condition is not met, and you want to do something else? That's where \`else\` comes in. The \`else\` block catches anything that wasn't caught by the \`if\`.\n\n\`\`\`python\nage = 15\nif age >= 18:\n    print("Access granted.")\nelse:\n    print("Access denied. You are too young.")\n\`\`\`\n\nIf you have multiple specific conditions to check, you use \`elif\` (short for "else if"). You can chain as many \`elif\` blocks as you need. Python will evaluate them from top to bottom. As soon as it finds one that is \`True\`, it executes that block and skips the rest.\n\n\`\`\`python\nscore = 85\nif score >= 90:\n    print("Grade: A")\nelif score >= 80:\n    print("Grade: B")\nelif score >= 70:\n    print("Grade: C")\nelse:\n    print("Grade: F")\n\`\`\`\n\n### Nested Conditions\nSometimes you need to check a condition inside another condition. This is called nesting. You simply add another layer of indentation. However, be careful not to nest too deeply, as it makes your code very hard to read.\n\n\`\`\`python\nis_logged_in = True\nhas_admin_rights = True\n\nif is_logged_in:\n    print("Welcome back!")\n    if has_admin_rights:\n        print("Entering Admin Control Panel")\n    else:\n        print("Entering User Dashboard")\nelse:\n    print("Please log in.")\n\`\`\``
                    },
                    {
                        title: "The Power of Loops: For and While", type: "lesson", order: 2,
                        content: `## The Power of Loops: For and While\n\n### Automating Repetition\nA core principle of software engineering is DRY: Don't Repeat Yourself. If you need to print the numbers 1 through 100, you shouldn't write 100 \`print()\` statements. Instead, you use a loop. A loop allows you to instruct the computer to execute a block of code repeatedly, as long as a certain condition remains true, or for every item in a collection.\n\nThere are two primary styles of loops in Python: the \`while\` loop and the \`for\` loop. They serve different purposes and choosing the right one is an important skill.\n\n### The While Loop\nA \`while\` loop is condition-based. It evaluates a Boolean condition before every iteration. If the condition is \`True\`, it runs the code block. Once the block is finished, it checks the condition again. It repeats this pattern indefinitely until the condition becomes \`False\`.\n\nThis makes \`while\` loops perfect for situations where you don't know in advance exactly how many times you need to loop. For example, a video game's main engine runs on an infinite \`while\` loop that continues until the player quits.\n\n\`\`\`python\ncountdown = 5\nwhile countdown > 0:\n    print(f"T-Minus {countdown}...")\n    countdown -= 1  # Crucial: Without this, the loop never ends!\nprint("Liftoff!")\n\`\`\`\n**Infinite Loops:** If you forget to update the variable controlling the condition, the condition will never become false, and your program will get stuck in an infinite loop. This will consume 100% of your CPU core until it is forcefully terminated.\n\n### The For Loop\nA \`for\` loop in Python is sequence-based. Instead of running based on a condition, it iterates over the items of a sequence (like a string, a list, or a range of numbers) one by one. It is highly predictable and prevents accidental infinite loops.\n\nIf you know exactly how many times a loop should run, or if you need to process every item in a collection, you should use a \`for\` loop.\n\n\`\`\`python\nmessage = "Hello"\n# This loop runs exactly 5 times, once for each letter\nfor letter in message:\n    print(f"Give me a {letter.upper()}!")\n\`\`\`\n\n### The Range Function\nWhen you just want to repeat an action a specific number of times, rather than looping over a string, you pair the \`for\` loop with Python's built-in \`range()\` function. \n\nThe \`range(start, stop, step)\` function generates a sequence of numbers. Note that the \`stop\` value is exclusive. \`range(5)\` generates numbers 0, 1, 2, 3, 4.\n\n\`\`\`python\n# Prints 0 through 4\nfor i in range(5):\n    print("Loop instance:", i)\n\n# Prints 10, 8, 6, 4, 2\nfor i in range(10, 0, -2):\n    print(i)\n\`\`\``
                    },
                    {
                        title: "Advanced Loop Control", type: "lesson", order: 3,
                        content: `## Advanced Loop Control\n\n### Interrupting the Normal Flow\nBy default, a loop executes its block fully for every iteration until the sequence is exhausted or the \`while\` condition becomes false. However, real-world problems often require us to break the rules. What if we are searching a massive database of a million users, and we find the user we are looking for on the 10th iteration? It would be a massive waste of processing power to continue looping through the remaining users.\n\nPython gives us special keywords to manipulate the flow of a loop from the inside: \`break\`, \`continue\`, and \`else\`.\n\n### The Break Statement\nThe \`break\` keyword acts as an emergency exit for your loop. The moment Python encounters a \`break\` statement, it immediately terminates the entire loop, skipping any remaining iterations regardless of the original condition. The program's execution jumps to the first line of code following the loop block.\n\n\`\`\`python\n# A common pattern: An infinite loop waiting for user exit\nwhile True:\n    command = input("Enter command (or type 'quit' to exit): ")\n    if command == "quit":\n        print("Exiting system...")\n        break  # Escapes the infinite while loop instantly\n    print(f"Executing command: {command}")\n\`\`\`\n\n### The Continue Statement\nWhile \`break\` destroys the loop entirely, \`continue\` acts more like a "skip" button. When Python hits a \`continue\` statement, it immediately aborts the *current* iteration of the loop, bypassing the remaining code in the block, and jumps straight back to the top of the loop to begin the *next* iteration.\n\nThis is extremely useful for filtering out bad data.\n\`\`\`python\n# Print only odd numbers\nfor i in range(1, 10):\n    if i % 2 == 0:\n        # If the number is even, skip the rest of this iteration\n        continue\n    print(f"Found an odd number: {i}")\n\`\`\`\n\n### The Loop Else Clause (Python's Hidden Gem)\nPython has a very unique feature that most other languages lack: you can attach an \`else\` block directly to a \`for\` or \`while\` loop! \n\nThis \`else\` block will execute if and only if the loop finishes entirely naturally—meaning it wasn't interrupted by a \`break\` statement. This is primarily used for search algorithms.\n\n\`\`\`python\nsecret_code = "1234"\nattempts = 3\n\nfor i in range(attempts):\n    guess = input("Enter code: ")\n    if guess == secret_code:\n        print("Vault opened!")\n        break\nelse:\n    # This only runs if the loop finished all iterations naturally\n    print("Intruder detected! Alarm triggered!")\n\`\`\``
                    },
                    {
                        title: "Control Flow Quiz", type: "quiz", order: 4,
                        questions: [
                            { question: "What does the `break` keyword do inside a loop?", options: ["It pauses the loop temporarily", "It immediately terminates the entire loop", "It skips the current iteration and goes to the next", "It throws an error"], correctAnswer: 1 },
                            { question: "What does the `continue` keyword do inside a loop?", options: ["It skips the current iteration and goes to the next loop cycle", "It exits the loop entirely", "It restarts the entire program", "It reverses the loop direction"], correctAnswer: 0 },
                            { question: "When does an `else` block attached to a loop execute?", options: ["After every single iteration", "Only if the loop was stopped by a break statement", "Only if the loop completes all iterations naturally without encountering a break", "Instead of the loop block if the condition is false immediately"], correctAnswer: 2 },
                            { question: "If you need a loop to run exactly 10 times, what should you use?", options: ["while True", "for i in range(10)", "while count == 10", "for i in list(10)"], correctAnswer: 1 }
                        ]
                    }
                ]
            },
            {
                title: "Mini Projects — Control Flow", order: 2,
                nodes: [
                    {
                        title: "Project Guide: Guess the Number Game", type: "lesson", order: 1,
                        content: `## Project Guide: Guess the Number Game\n\n### Introduction\nThe "Guess the Number" game is a classic programming rite of passage. In this project, you will build an interactive game where the computer thinks of a random number between 1 and 100, and the player repeatedly guesses until they get it right. Along the way, the program will provide hints ("Too high!" or "Too low!").\n\n### Goal\nCreate a Python script that utilizes a \`while\` loop, conditionals (\`if/elif/else\`), and user input to facilitate a guessing game. The game should track the number of attempts the user takes to find the correct answer.\n\n### Requirements\n1. Import the \`random\` module at the top of your file to generate random numbers.\n2. Initialize a secret number between 1 and 100.\n3. Initialize an \`attempts\` counter variable at 0.\n4. Set up an infinite \`while\` loop.\n5. Inside the loop, prompt the user for their guess and increment the \`attempts\` counter.\n6. Check the guess against the secret number, providing appropriate print statements.\n7. Break out of the loop when the user guesses correctly, and congratulate them.\n\n### Step-by-Step Guide\n\n**Step 1: The Setup**\nAt the very top of your script, write \`import random\`. This brings in a massive library of randomization tools. To safely generate an integer between 1 and 100, use \`secret_number = random.randint(1, 100)\`. Set an \`attempts\` variable to 0.\n\n**Step 2: The Loop**\nBecause you have no idea how long the user will take, use a generic infinite loop: \`while True:\`. Make sure everything that follows is indented under this loop.\n\n**Step 3: Taking Input**\nInside the loop, ask the user for a guess using \`input()\`. Since \`input()\` returns a string, you must wrap it in \`int()\` immediately so you can do math operations on it later. Don't forget to increase your \`attempts\` variable by 1 using \`+= 1\`.\n\n**Step 4: The Logic**\nBuild an \`if/elif/else\` structure. \n- \`if\` the guess is less than the secret number, print "Too low! Try again."\n- \`elif\` the guess is greater than the secret number, print "Too high! Try again."\n- \`else\` (which logically means they guessed it perfectly), print a victory message utilizing an f-string to display how many attempts they took, and then use the \`break\` keyword to shatter the loop and end the game.\n\n### Challenge Yourself\n- Instead of an infinite loop, give the player a strict maximum of 7 lives. If they fail 7 times, break the loop and print a Game Over message displaying the secret number.\n- Add input validation using a \`try/except\` block to prevent the game from crashing if the user types a word instead of a number.`
                    },
                    {
                        title: "Project Guide: Password Strength Checker", type: "lesson", order: 2,
                        content: `## Project Guide: Password Strength Checker\n\n### Introduction\nSecurity is a massive part of modern software engineering. Every time you sign up for a website, an algorithm checks your chosen password to ensure it meets certain security criteria. In this project, you will write a script that analyzes a user's password and scores its strength using conditions and sequence loops.\n\n### Goal\nBuild an application that takes a password attempt from the user and analyzes it across four categories: length, presence of uppercase letters, presence of lowercase letters, and presence of numbers. It will award a "score" to the password and output a final grade.\n\n### Requirements\n1. Accept a string input from the user.\n2. Initialize a \`score\` variable to 0.\n3. Check the length of the string. If it is 8 characters or longer, add 1 to the score.\n4. Iterate through every character in the password using a \`for\` loop to check for the presence of uppercase, lowercase, and numeric characters. Award points accordingly.\n5. Evaluate the final score and print whether the password is "Weak", "Moderate", or "Strong".\n\n### Step-by-Step Guide\n\n**Step 1: Collecting the Target**\nPrompt the user for a password using \`input()\`. Initialize a variable \`score = 0\`. Also initialize three boolean tracking variables: \`has_upper = False\`, \`has_lower = False\`, and \`has_digit = False\`.\n\n**Step 2: Checking Length**\nUse a standalone \`if\` statement to check if the \`len()\` of the password string is \`>= 8\`. If it is, \`score += 1\`.\n\n**Step 3: The For Loop Analysis**\nString variables are sequences, meaning you can loop through them. Write a \`for char in password:\` loop. Inside the loop, you need to test the individual character. \nPython strings have helpful built-in methods designed exactly for this.\n- Use an \`if\` statement to check if \`char.isupper()\`. If True, set \`has_upper = True\`.\n- Use another \`if\` to check if \`char.islower()\`. If True, set \`has_lower = True\`.\n- Use a final \`if\` to check if \`char.isdigit()\`. If True, set \`has_digit = True\`.\n\n**Step 4: Tallying the Score**\nOnce the \`for\` loop finishes, test your boolean tracker variables. If \`has_upper\` is True, add 1 to the score. Do the same for \`has_lower\` and \`has_digit\`. Your maximum possible score is 4.\n\n**Step 5: The Final Grade**\nBuild a final \`if/elif/else\` chain. If the \`score\` is 4, print "Strong Password". If the score is 3, print "Moderate Password - Consider adding variance". For anything else, print "Weak Password - Must contain caps, lowercase, numbers, and be at least 8 chars long."\n\n### Challenge Yourself\n- Add a 5th scoring category that checks for special characters (like \`@\`, \`#\`, \`$\`, \`!\`).\n- Instead of rejecting weak passwords, utilize an infinite \`while\` loop to force the user to keep trying until they formulate a password that achieves a perfect score of 4.`
                    }
                ]
            }
        ]
    },

    {
        title: "Functions and Modular Programming", order: 5,
        chapters: [
            {
                title: "Reusable Code with Functions", order: 1,
                nodes: [
                    {
                        title: "Defining and Calling Functions", type: "lesson", order: 1,
                        content: `## Reusable Code with Functions\n\n### The Problem of Repetition\nAs your programs grow larger, you will inevitably find yourself writing the exact same blocks of code in multiple different places. Say you are building a text-based RPG game. Every time a player takes damage, you need to calculate their armor mitigation, reduce their health, print a notification to the screen, and check if they have died. If you have 50 different enemies taking attacks from 10 different weapons, copy-pasting that damage calculation logic everywhere will result in an unmaintainable nightmare. What happens if you discover a bug in the math? You would have to locate and fix it in 500 different copied locations.\n\nThis is where **functions** come in. A function is a named block of code that performs a specific, isolated task. You write the logic exactly once, give it a name, and then you "call" it from anywhere in your program as many times as you like. \n\n### Defining a Function\nIn Python, we use the \`def\` keyword (short for define) to create a function. You follow \`def\` with the name of the function, a set of parentheses \`()\`, and a colon \`:\`. Next, you indent the code that belongs inside the function.\n\n\`\`\`python\ndef print_greeting():\n    print("Hello, user!")\n    print("Welcome to the main menu.")\n\`\`\`\nWhen Python reads this definition, it does *not* execute the code inside. It simply saves it in memory under the name \`print_greeting\`.\n\n### Calling a Function\nTo actually run the code inside your function, you must "call" or "invoke" it. You do this by typing the function's name followed by parentheses.\n\n\`\`\`python\n# Calling the function multiple times\nprint_greeting()\nprint_greeting()\n\`\`\`\n\n### Passing Data: Parameters and Arguments\nA function that only ever does the exact same thing is not very flexible. We can make functions dynamic by passing data into them. We define variables inside the parentheses called **parameters**. When calling the function, the actual data values we pass in are called **arguments**.\n\n\`\`\`python\n# 'player_name' and 'damage' are parameters\ndef take_damage(player_name, damage):\n    print(f"{player_name} took {damage} points of damage!")\n\n# 'Arthas' and 45 are arguments\ntake_damage("Arthas", 45)\ntake_damage("Uther", 12)\n\`\`\`\nBy utilizing parameters, one single block of logic can handle an infinite number of different scenarios.`
                    },
                    {
                        title: "Return Values and Variable Scope", type: "lesson", order: 2,
                        content: `## Return Values and Variable Scope\n\n### Sending Data Back: The Return Statement\nMany functions don't just perform an action (like printing to the screen); they compute a result and send it back to the part of the program that called the function. You accomplish this using the \`return\` keyword.\n\nWhen Python executes a \`return\` statement, two things happen immediately:\n1. The function is instantly terminated. No code below the \`return\` statement in that function will ever be executed.\n2. The evaluated result is handed back to the caller.\n\n\`\`\`python\ndef calculate_area(length, width):\n    area = length * width\n    return area\n    print("This will never print!") # Unreachable code\n\n# We must store the returned value in a variable\nbedroom_area = calculate_area(12, 10)\nprint(f"Your bedroom is {bedroom_area} square feet.")\n\`\`\`\nA function that does not possess an explicit \`return\` statement automatically returns a special Python object called \`None\`.\n\n### The Concept of Scope\nOne of the most confusing concepts for beginners is **Variable Scope**. The scope of a variable defines where in your program that variable can be accessed or modified.\n\nWhen you create a variable inside a function, it is a **Local Variable**. It exists *only* inside that function, and it is completely destroyed the moment the function finishes running. The outside world cannot see it.\n\n\`\`\`python\ndef make_coffee():\n    cup_size = "Large"  # Local variable\n    print(f"Brewing a {cup_size} coffee.")\n\nmake_coffee()\n# print(cup_size)  # ERROR! NameError: name 'cup_size' is not defined\n\`\`\`\n\nVariables created outside of any function are called **Global Variables**. They can be read by any function in the entire file. However, modifying a global variable from inside a function is generally discouraged because it leads to unpredictable "side effects." By default, if you try to modify a global variable inside a function, Python will instead create a brand new local variable with the exact same name, leaving the global one untouched.\n\n\`\`\`python\nplayer_score = 0  # Global variable\n\ndef increase_score():\n    # This creates a LOCAL variable called player_score! \n    # It does NOT change the global one unless you explicitly use the 'global' keyword.\n    player_score = 100 \n\nincrease_score()\nprint(player_score)  # Still outputs 0\n\`\`\`\nMastering scope ensures your functions remain isolated, predictable, and bug-free.`
                    },
                    {
                        title: "Default and Keyword Arguments", type: "lesson", order: 3,
                        content: `## Default and Keyword Arguments\n\n### Enhancing Function Flexibility\nAs you build more complex systems, you'll find that some functions require a large number of parameters. However, in many cases, those parameters will have the exact same value 90% of the time. Python offers powerful tools to handle these scenarios elegantly: Default Arguments and Keyword Arguments.\n\n### Default Arguments\nYou can assign a default value to a parameter directly in the function definition. If the programmer calling the function decides not to provide an argument for that parameter, Python will automatically substitute the default value. If the programmer does provide a value, it overrides the default.\n\nThis is incredible for optional settings.\n\`\`\`python\n# 'discount' has a default value of 0.0\n# 'tax_rate' has a default value of 0.08 (8%)\ndef calculate_total(price, discount=0.0, tax_rate=0.08):\n    discounted_price = price * (1 - discount)\n    final_total = discounted_price * (1 + tax_rate)\n    return round(final_total, 2)\n\n# Caller only provides the required 'price' parameter.\n# The function uses discount=0.0 and tax_rate=0.08 automatically.\nstandard_checkout = calculate_total(100.00)\n\n# Caller provides two arguments. \n# 100 goes to price, 0.20 overrides the discount. tax_rate remains 0.08.\nsale_checkout = calculate_total(100.00, 0.20)\n\`\`\`\n**Important Rule:** In your function definition, all parameters with default values must be placed at the very end of the parameter list. You cannot place a required parameter after a default parameter.\n\n### Keyword Arguments\nWhen calling a function normally, arguments are mapped to parameters based purely on their position from left to right. These are called *Positional Arguments*.\n\nHowever, when a function has many parameters, it becomes impossible for a human to remember what order they must be passed in. Python allows you to explicitly name your arguments when calling the function. These are called **Keyword Arguments**. \n\nUsing keyword arguments means the physical order of the arguments no longer matters, and it makes your function calls incredibly readable.\n\n\`\`\`python\ndef create_profile(username, age, country, is_premium):\n    print(f"Profile: {username}, age {age}, from {country}. Premium: {is_premium}")\n\n# Using Positional Arguments (Hard to read)\ncreate_profile("NinjaStar", 25, "Japan", True)\n\n# Using Keyword Arguments (Order doesn't matter, self-documenting)\ncreate_profile(\n    is_premium=True,\n    username="NinjaStar",\n    country="Japan",\n    age=25\n)\n\`\`\`\nBy combining default arguments and keyword arguments, you empower the developers using your functions to write highly readable and flexible code.`
                    },
                    {
                        title: "Functions Quiz", type: "quiz", order: 4,
                        questions: [
                            { question: "What is the primary purpose of writing functions?", options: ["To make the code run faster", "To reduce code repetition and improve maintainability", "To encrypt user data", "To declare global variables"], correctAnswer: 1 },
                            { question: "What happens immediately after a function executes a `return` statement?", options: ["The function skips to the next loop", "The function terminates instantly and passes back the evaluating value", "The program crashes", "The function prints the value to the console"], correctAnswer: 1 },
                            { question: "Can a function see a variable that was created inside a completely different function?", options: ["Yes, variables inside functions are global", "Yes, if you use the export keyword", "No, local variables are strictly isolated to the function they were created in", "Only if the function is inside a try/except block"], correctAnswer: 2 },
                            { question: "Where must default parameters be placed in a function definition?", options: ["Before any required parameters", "At the very end of the parameter list", "In the exact middle of the list", "It does not matter"], correctAnswer: 1 }
                        ]
                    }
                ]
            },
            {
                title: "Mini Projects — Functions", order: 2,
                nodes: [
                    {
                        title: "Project Guide: Text Analyzer Tool", type: "lesson", order: 1,
                        content: `## Project Guide: Text Analyzer Tool\n\n### Introduction\nFunctions shine when they wrap complex operations into a neat, reusable package. In this project, you will build your own library of text analysis functions. You will define several distinct functions that each perform a specialized analysis on an input string, and then tie them all together into a master report.\n\n### Goal\nWrite a program consisting of multiple custom functions designed to dissect a paragraph of text. One function will count vowels, another will count total words, and a third will find the longest individual word. You will call these functions and generate a readable analysis report.\n\n### Requirements\n1. Define a function \`count_vowels(text)\` that returns an integer.\n2. Define a function \`count_words(text)\` that returns an integer.\n3. Define a function \`find_longest_word(text)\` that returns a string.\n4. Prompt the user for a large block of text.\n5. Invoke all three functions, passing the user's text as the argument.\n6. Print a formatted report with the returned values.\n\n### Step-by-Step Guide\n\n**Step 1: The Word Counter**\nDefine \`def count_words(text):\`. The easiest way to count words in a string is to use the \`.split()\` method, which breaks a string into a list of words automatically by splitting at the empty spaces. Then, simply \`return\` the \`len()\` of that resulting list.\n\n**Step 2: The Vowel Counter**\nDefine \`def count_vowels(text):\`. Initialize a local variable \`vowel_count = 0\`. Use a \`for\` loop to evaluate \`for char in text.lower():\`. Check if the character \`is in\` the string \`"aeiou"\`. If it is, increment the counter. \`return\` the final count at the end.\n\n**Step 3: The Longest Word Finder**\nDefine \`def find_longest_word(text):\`. First, split the text into a list just like you did in Step 1. Initialize an empty string \`longest = ""\`. Loop through every word in the created list. For each word, compare its length \`len(word)\` against the length of your tracker \`len(longest)\`. If the current word is longer, reassign \`longest = word\`. Once the loop completes, returning \`longest\` will give you the largest word in the text.\n\n**Step 4: Execution**\nAsk the user for input: \`sample_text = input("Enter a paragraph: ")\`.\nCall your functions, ensuring you save their returned outputs into variables! Example: \`words = count_words(sample_text)\`.\nFinally, use a multi-line f-string to print a beautiful, readable report outputting the \`words\`, the \`vowels_found\`, and the \`longest_str\`.\n\n### Challenge Yourself\n- Add a new function that counts the frequency of a *specific* target letter provided by the user (meaning the function will need *two* parameters: the text and the target letter).\n- Expand your longest word finder to strip away punctuation marks like commas or periods before calculating the word's true length.`
                    },
                    {
                        title: "Project Guide: Caesar Cipher Encryption", type: "lesson", order: 2,
                        content: `## Project Guide: Caesar Cipher Encryption\n\n### Introduction\nThe Caesar Cipher is one of the oldest encryption techniques in history, famously used by Julius Caesar to encode military messages. It is a substitution cipher where each letter in the original message is shifted forward downwards along the alphabet by a certain number of spaces. For example, with a shift of 1, 'A' becomes 'B', and 'Z' wraps around and becomes 'A'.\n\n### Goal\nYou will build a dynamic encryption tool using functions. You will create an \`encrypt_message(text, shift)\` function that transforms English strings into secret codes based on the mathematical manipulation of alphabet character mapping.\n\n### Requirements\n1. Define a function taking two arguments: the message, and an integer shift value.\n2. Utilize a list or string of the alphabet mapping.\n3. Process each character of the message, applying the shift exclusively to alphabetical characters while leaving spaces and punctuation completely untouched.\n4. Ensure the shift elegantly wraps around from 'Z' back to 'A' using the modulo \`%\` operator.\n5. Return the newly encrypted string.\n\n### Step-by-Step Guide\n\n**Step 1: Define the Function Signature**\nWrite \`def encrypt_message(text, shift_amount):\`. Make sure you give \`shift_amount\` a sensible default value (like 3) using default argument syntax, so the encryption works even if the user forgets to specify a shift. \n\n**Step 2: Setup the Alphabet**\nInside the function, define a local constant string containing the alphabet: \`alphabet = "abcdefghijklmnopqrstuvwxyz"\`. Create an empty string \`encrypted_text = ""\` to collect the new characters.\n\n**Step 3: The Transformation Loop**\nUse a \`for\` loop to iterate over each \`char\` in the lowercased version of the input \`text.lower()\`. \nIf the \`char\` is present in the \`alphabet\` string, find its numerical index using \`alphabet.index(char)\`. \nTo calculate its new shifted location, add the \`shift_amount\`. However, you must prevent the index from exceeding 25 (the end of the alphabet). Use modulo arithmetic for this: \`new_index = (current_index + shift_amount) % 26\`.\nExtract the new character: \`new_char = alphabet[new_index]\`, and append it to your \`encrypted_text\`.\n\n**Step 4: Handling Non-Letters**\nWhat if the character is a space, question mark, or number? The cipher should ignore them. Use an \`if/else\` block based around \`if char in alphabet:\`. If it is not in the alphabet, simply append the raw \`char\` to \`encrypted_text\` without altering it. Ensure you \`return encrypted_text\` exactly once, at the very end of the function body.\n\n**Step 5: Testing**\nCreate a testing block at the bottom of the file. Have the user input a secret message to encode, and pass it to your function. Check the results!\n\n### Challenge Yourself\n- Build a companion \`decrypt_message(text, shift_amount)\` function. (*Hint: Mathematically, decryption is literally the exact same process as encryption, but with the negative inverse of the shift!*).\n- Give your function an optional boolean \`preserve_casing\` keyword argument. If true, the output maintains the uppercase letters of the original message instead of making everything lowercase.`
                    }
                ]
            }
        ]
    }
];
