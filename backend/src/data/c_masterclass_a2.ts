// C Masterclass — Part A2 (Sections 4-5)
export const cMasterclassPartA2 = [
    {
        title: "Control Flow in C", order: 4,
        chapters: [
            {
                title: "Conditional Logic and Branching", order: 1,
                nodes: [
                    {
                        title: "If, Else If, and Switch Statements", type: "lesson", order: 1,
                        content: `## If, Else If, and Switch Statements\n\n### The Need for Decision Making\nA software program that strictly executes every line of code from top to bottom every single time is fundamentally useless. A calculator needs to choose whether to add or subtract based on user input. A game engine must decide whether to reduce player health depending on if an attack connected. We control the flow of execution using conditional statements.\n\n### The If/Else Structure\nThe \`if\` statement is the backbone of control flow. It evaluates a mathematical expression to determine if it is "True" or "False". In C, any number that is not zero is considered True. Zero is False.\n\n\`\`\`c\nint age = 20;\n\n// If the condition is met, execute the attached block of code.\nif (age >= 18) {\n    printf("You are an adult.\\n");\n} else if (age >= 13) {\n    printf("You are a teenager.\\n");\n} else {\n    // The fallback block if everything else fails.\n    printf("You are a child.\\n");\n}\n\`\`\`\nNotice the strict usage of curly braces \`{}\`. While C allows you to omit them if the block contains only one single line of code, doing so is highly discouraged in modern engineering as it routinely causes catastrophic logic bugs during future edits. Always use braces.\n\n### The Switch Statement\nWhen you need to check a single integer or character against a massive list of exact, specific possibilities, chaining twenty \`else if\` statements becomes incredibly messy and inefficient. \n\nC offers the \`switch\` statement, an extremely optimized branching mechanism specifically designed for equality checking against constants.\n\n\`\`\`c\nint menu_choice = 2;\n\nswitch (menu_choice) {\n    case 1:\n        printf("Starting New Game...\\n");\n        break; // CRITICAL: You must break out!\n    case 2:\n        printf("Loading Saved Game...\\n");\n        break;\n    case 3:\n        printf("Opening Settings...\\n");\n        break;\n    default:\n        // Equivalent to the final 'else'\n        printf("Invalid choice!\\n");\n        break;\n}\n\`\`\`\n\n### The Fallthrough Trap\nNotice the \`break;\` statement. If you forget to include a \`break;\` at the end of a \`case\`, C doesn't stop executing. It instantly "falls through" into the very next case and executes its code as well, completely ignoring the case condition itself! While occasionally useful in hyper-specific scenarios, accidental fall-through is one of the most common causes of bugs in beginner C code.`
                    },
                    {
                        title: "While and Do-While Loops", type: "lesson", order: 2,
                        content: `## While and Do-While Loops\n\n### Repeating Operations\nIf a program needs to check 10,000 files for a virus, writing 10,000 separate checker statements is unacceptable. Software engineering requires loops to automate repetitive tasks based on condition monitoring.\n\n### The Standard While Loop\nThe \`while\` loop evaluates a condition natively before the cycle begins. If the condition evaluates to true (non-zero), the loop executes the internal block. Once the block finishes, the loop leaps back up to the top and re-evaluates the condition securely. It will repeat this perpetually until the condition eventually evaluates to false (zero).\n\n\`\`\`c\nint fuel = 3;\n\nwhile (fuel > 0) {\n    printf("Thrusters firing! Fuel remaining: %d\\n", fuel);\n    fuel--; // We must decrement the fuel, or we enter an infinite loop!\n}\nprintf("Out of fuel. Drifting.\\n");\n\`\`\`\nIf the condition is initially false (if fuel started at 0), the \`while\` loop completely bypasses its block, meaning the operation executes zero times.\n\n### The Guarded Terminal Issue\nWhen building command-line menus, \`while\` loops can be awkward. You often want to ask the user a question first, and *then* evaluate their response to see if you should ask again. A standard \`while\` loop evaluates *before* it asks.\n\n### The Do-While Loop\nC solves this interactive problem with the \`do-while\` loop. The core difference is architectural: the \`do-while\` loop unconditionally executes its code block exactly once *before* it ever checks the termination condition at the bottom.\n\n\`\`\`c\nint menu_selection;\n\ndo {\n    printf("1. Play\\n");\n    printf("2. Exit\\n");\n    printf("Make your selection: ");\n    scanf("%d", &menu_selection);\n    \n    // Look down here! The condition is at the bottom!\n} while (menu_selection != 2); \n\nprintf("Exiting application.\\n");\n\`\`\`\nBecause the condition evaluates at the very end of the cycle, a \`do-while\` loop is absolutely guaranteed to execute at least one time, regardless of the starting numerical state of the program. Notice the mandatory semicolon at the end of the \`while()\` condition—a syntax quirk unique to the \`do-while\` loop.`
                    },
                    {
                        title: "For Loops and Loop Control", type: "lesson", order: 3,
                        content: `## For Loops and Break/Continue\n\n### The Fully Controlled Cycle\nA \`while\` loop is an unstructured beast. It relies heavily on you remembering to increment internal variables manually, otherwise it spins wildly out of control. When you know precisely how many times an operation must execute, the \`for\` loop is undeniably the superior structural choice.\n\n### Dissecting the For Loop\nThe \`for\` loop consolidates all loop-management mechanics (Initialization, Condition, and Iteration Control) directly into one unified header statement, cleanly segregated by semicolons.\n\n\`\`\`c\n// Initializer ; Condition ; Step Modifier\nfor (int i = 0; i < 5; i++) {\n    printf("Iteration number: %d\\n", i);\n}\n\`\`\`\n1. \`int i = 0;\` executes precisely once before the loop begins, establishing the tracking variable.\n2. \`i < 5;\` evaluates strictly before every single loop iteration to determine if the cycle continues.\n3. \`i++\` executes immediately after the internal block finishes, smoothly modifying the tracker.\nBecause everything is handled in the header, you never accidentally forget to increment your variable.\n\n### Premature Ejection: The 'break' Statement\nOften, loop termination relies on dynamic events occurring inside the block, not necessarily the header condition. If you are scanning 100 folders for a specific image and find it in folder 3, continuing to loop through the remaining 97 folders wastes incredible amounts of processing power. \n\nThe \`break\` keyword acts as an emergency escape pod. The instant the compiler strikes a \`break\` instruction, it immediately aborts the loop entirely, disregarding the condition, and execution drops straight down to the line of code directly underneath the loop.\n\n### Bypassing Iterations: The 'continue' Statement\nWhat if you don't want to destroy the entire loop? What if you merely want to skip processing the *current* iteration? The \`continue\` keyword acts like a filter bypass. Upon encountering \`continue\`, the compiler aborts the rest of the internal block, instantly triggers the loop's Step Modifier (\`i++\`), and cleanly leaps back to the top to process the *next* iteration continuously.\n\n\`\`\`c\nfor (int check = 1; check <= 5; check++) {\n    if (check == 3) {\n        continue; // Bypasses the number 3 entirely\n    }\n    printf("Processing %d\\n", check);\n}\n// Output: 1, 2, 4, 5\n\`\`\``
                    },
                    {
                        title: "Control Flow Quiz", type: "quiz", order: 4,
                        questions: [
                            { question: "Why must you typically include a `break` statement at the end of every `case` inside a `switch` block?", options: ["Because the compiler demands it structurally", "To completely prevent 'fall-through', where C uncontrollably executes the code belonging to subsequent cases", "Because it resets the main variable to zero", "To close the program smoothly"], correctAnswer: 1 },
                            { question: "What is the primary defining characteristic setting a `do-while` loop apart from a standard `while` loop?", options: ["Do-while loops process significantly faster", "Do-while loops evaluate the condition at the end, guaranteeing the contained block executes at minimum one time", "Do-while loops cannot utilize the `break` command", "Do-while loops process backwards"], correctAnswer: 1 },
                            { question: "Inside a `for` loop declaration `for(int i=0; i<5; i++)`, exactly when does the `i++` operation run?", options: ["Immediately before the condition is evaluated", "It only runs if the loop successfully terminates", "Immediately after the internal code block finishes its cycle, right before the next condition check", "Randomly"], correctAnswer: 2 },
                            { question: "How does the `continue` keyword alter loop execution?", options: ["It terminates the loop utterly and skips to the next program phase", "It aborts the current iteration cycle early, skips any remaining logic in the block, and seamlessly jumps to construct the next iteration", "It pauses the loop until a user clicks a button", "It forcibly resets the loop tracker to zero"], correctAnswer: 1 }
                        ]
                    }
                ]
            },
            {
                title: "Mini Projects — Control Flow", order: 2,
                nodes: [
                    {
                        title: "Project Guide: Advanced Calculator Menu", type: "lesson", order: 1,
                        content: `## Project Guide: Advanced Calculator Menu\n\n### Introduction\nA powerful CLI (Command Line Interface) thrives on perpetual interactivity. In this capstone project for Section 4, you will construct an infinitely looped calculator application incorporating a \`do-while\` loop for the graphical interface and a \`switch\` statement for processing mathematically complex mathematical branching logic.\n\n### Goal\nEngineer an interactive application that continuously prompts the user with a stylized numbered text menu. Ensure the application intercepts mathematical operations accurately utilizing \`switch\` statement evaluations. The program must explicitly protect the user from dividing by zero using conditional \`if\` statements.\n\n### Requirements\n1. Establish a \`do-while\` loop forming the shell of the application.\n2. Imprint a text menu presenting five distinct options: [1] Addition, [2] Subtraction, [3] Multiplication, [4] Division, and [5] Exit.\n3. Request and intercept the user's operational choice using \`scanf\`.\n4. If the choice necessitates math (Options 1-4), strictly request two additional floating-point numerals from the user.\n5. Deploy a \`switch\` architectural block evaluating the initial choice to handle the execution routes.\n6. For Division (Case 4), deploy an \`if\` guard specifically verifying the denominator is not equal to \`0.0\`. Refuse the operation and print an error violently if a mathematically impossible zero division is attempted.\n\n### Step-by-Step Guide\n\n**Step 1: The Loop Shell**\nDeclare an \`int choice;\`. Initialize your \`do {\` framework. Populate it with \`printf\` strings rendering the text menu visually. Terminate the structure securely with \`} while (choice != 5); \`. This ensures perfect infinite repeating until the user demands the exit sequence.\n\n**Step 2: The Numerical Intercept**\nWithin the loop, prompt the user: \`scanf("%d", & choice); \`. \nInstantly establish a secondary conditional evaluating \`if (choice >= 1 && choice <= 4)\`. If true, initiate a secondary interception, prompting the user for exactly two float variables: \`scanf("%f %f", & num1, & num2); \`.\n\n**Step 3: The Switch Routing Network**\nConstruct your \`switch (choice) {\` block. Produce \`case 1: \`, \`case 2: \`, etc. Inside each case, print the mathematical operation. Crucially, strictly conclude every single case block with a definitive \`break; \` command to avoid bleeding into other cases.\n\n**Step 4: Division Guard Mechanisms**\nInside \`case 4: \`, generate an \`if\` statement measuring the denominator: \`if (num2 == 0.0f)\`. If true, transmit an overriding \`printf("Error: Division by zero is impossible.\\n"); \`. \nStructure an interwoven \`else \` containing the legitimate division calculation logic.\n\n### Challenge Yourself\n- Construct a robust \`default: \` branch within the switch statement that catches unexpected input (like typing a \`6\`) and alerts the user.\n- Instead of utilizing purely numbers for the menu, re-architect the entire system to use a single character for input (e.g., \` + \`, \` - \`, \` * \`, \` / \`)! Your switch would then evaluate \`char\` datatypes like \`case '+': \`.`
          }
        ]
      }
    ]
  },

{
    title: "Functions and Modularity", order: 5,
        chapters: [
            {
                title: "Function Architecture and Scoping", order: 1,
                nodes: [
                    {
                        title: "Creating Functions in C", type: "lesson", order: 1,
                        content: `## Creating Functions in C\n\n### The Necessity of Abstraction\nImagine trying to read a 10,000-line \`main()\` function. It would be a nightmare. Software engineering fundamentally relies on **Modularity**—separating a massive problem into distinct, manageable, isolated operations. We achieve this isolation through **Functions**.\n\nA function is simply an independent block of reusable code tasked with doing one specific mathematical or logical job perfectly.\n\n### The Anatomy of a Function\nIn C, every function requires three critical architectural pillars:\n1. **Return Type:** Exactly what type of data is this function handing back when it finishes?\n2. **Name:** How do we uniquely call this operation?\n3. **Parameters:** What specific variables must we hand to the function for it to do its job?\n\n\`\`\`c\n// ReturnType FunctionName(Type Parameter1, Type Parameter2)\nint calculate_area(int length, int width) {\n    int final_area = length * width;\n    return final_area; // The data type strictly matches the 'int' return type!\n}\n\`\`\`\nIf a function purely prints something and mathematically returns absolutely nothing to the system, its return type must strictly be enforced as \`void\`.\n\n\`\`\`c\nvoid print_warning() {\n    printf("WARNING: Critical System Temperature!\\n");\n    // Notice there is no return statement here.\n}\n\`\`\`\n\n### Invoking Functions\nOnce defined, a function slumbers in memory doing absolutely nothing until explicitly called (invoked) by another operation (commonly your \`main\` function). When invoking a function, the arguments you pass in *must* perfectly identically match the data types determined in the parameters.\n\n\`\`\`c\nint main() {\n    // Invoking the void function\n    print_warning(); \n    \n    // Capturing the returned integer into a variable\n    int bedroom_area = calculate_area(12, 10); \n    printf("The area is %d square feet.\\n", bedroom_area);\n    \n    return 0;\n}\n\`\`\``
                    },
                    {
                        title: "Scope and Function Prototypes", type: "lesson", order: 2,
                        content: `## Variable Scope and Function Prototypes\n\n### The Compiler's Top-Down Mentality\nThe C compiler is highly rigid: it strictly reads a file from line 1 straight downwards. It is incredibly paranoid about unknown entities. If you attempt to invoke a function inside \`main()\` that isn't actually defined until line 100, the compiler reaches \`main()\`, has a panic attack because it hasn't seen the final function yet, and aggressively aborts the compilation with a fatal error!\n\nTo circumvent this architectural flaw, C requires **Function Prototypes** (also known as Function Declarations).\n\n### Function Prototypes\nA prototype acts as a specialized warning flag placed at the absolute top of your file. It explicitly tells the compiler: "Hey, I swear a function with this exact signature exists somewhere deep down below. Trust me, compile the \`main\` function anyway."\n\n\`\`\`c\n#include <stdio.h>\n\n// Prototype Definition (Notice the semicolon!)\nint add_numbers(int a, int b);\n\nint main() {\n    // The compiler allows this because it read the prototype above!\n    int result = add_numbers(5, 10); \n    return 0;\n}\n\n// The Actual Function Implementation\nint add_numbers(int a, int b) {\n    return a + b;\n}\n\`\`\`\n\n### Variable Scope: Local Isolation\nVariables declared inside a specific function (including its arguments) are inherently **Local Variables**. That means they strictly exist mathematically constrained *only* inside that specific internal block. The instant the function concludes executing via a \`return\` command, every single local variable is instantly, violently eradicated from physical RAM.\n\n\`\`\`c\nvoid perform_magic() {\n    int secret_number = 42; // Local variable\n}\n\nint main() {\n    perform_magic();\n    // FATAL ERROR! secret_number doesn't exist here! The compiler crashes.\n    // printf("%d", secret_number); \n}\n\`\`\`\n\n### Global Variables: A Dangerous Construct\nVariables defined entirely outside of any function block are **Global Variables**. They are accessible by any operation, anywhere in the program. This sounds highly convenient, but relying on globals is universally considered a catastrophic architectural sin in modern engineering. Any rogue function can modify a global seamlessly, making tracking down mathematical bugs almost statistically impossible in production applications.`
                    },
                    {
                        title: "Pass-by-Value vs. Pass-by-Reference", type: "lesson", order: 3,
                        content: `## Pass-by-Value vs. Pass-by-Reference\n\n### The Illusion of Modification\nA ubiquitous limitation beginners discover when working with C functions is attempting to build operations that mathematically modify the original variables passed to it.\n\nExamine this incredibly standard attempt to construct a "swapping" operation:\n\n\`\`\`c\nvoid swap(int a, int b) {\n    int temp = a;\n    a = b;\n    b = temp;\n}\n\nint main() {\n    int x = 10, y = 20;\n    swap(x, y); \n    printf("x=%d, y=%d", x, y); // Outputs: x=10, y=20\n}\n\`\`\`\nWhy is the output completely unchanged? Because C mathematically enforces **Pass-by-Value** exclusively. \n\nWhen we called \`swap(x, y)\`, C actually constructed two brand new, utterly isolated \`a\` and \`b\` variables inside the function's separate RAM block, and merely photocopied the values \`10\` and \`20\` into them. The function perfectly swapped the newly fabricated photocopies, and then instantly eradicated them upon returning! The original \`x\` and \`y\` securely remaining inside \`main\` were mathematically never touched.\n\n### Breaking the Isolation: Pass-by-Reference\nHow do we grant a remote function absolute authorization to overwrite variables residing inside \`main\`? We must abandon copying values and instead provide the function with the physical, literal coordinates of the variables. We achieve this specifically through **Pointers**.\n\nIf we re-architect the function to actively expect a Memory Address (a Pointer), it can remotely reach back into the \`main\` operation's RAM block and manipulate the original data strictly utilizing the Dereference \` * \` operator.\n\n\`\`\`c\n// Accept physical memory addresses utilizing pointer notation!\nvoid force_swap(int *a_ptr, int *b_ptr) {\n    int temp = *a_ptr;  // Read the data living at coordinate A\n    *a_ptr = *b_ptr;    // Overwrite the data at A with data residing at B\n    *b_ptr = temp;      // Overwrite the data at B with our temporary stash\n}\n\nint main() {\n    int x = 10, y = 20;\n    // Hand over the physical hardware Memory Addresses!\n    force_swap(&x, &y);\n    printf("x=%d, y=%d", x, y); // Outputs: x=20, y=10. Success!\n}\n\`\`\`\nUnderstanding this highly raw interaction is the ultimate separating line between a novice coder and an elite systems engineer.`
          },
            {
                title: "Functions Quiz", type: "quiz", order: 4,
                questions: [
                    { question: "What return type must explicitly be utilized if a function executes operations but inherently hands no mathematical value back to the compiler?", options: ["null", "void", "empty", "int"], correctAnswer: 1 },
                    { question: "Why are Function Prototypes inherently necessary in C?", options: ["Because they encrypt identical function clones", "They provide advanced warning to the incredibly rigid top-down compiler that the function absolutely exists later in the file, preventing a fatal crash", "They establish complex graphical user interfaces inherently", "Because they drastically shrink the final hardware compilation footprint"], correctAnswer: 1 },
                    { question: "What explicitly occurs internally when variables are passed into a standard function through Pass-by-Value mechanics?", options: ["The function actively deletes the original variable instantly", "The compiler permanently moves the variable into the new function", "The compiler fabricates an isolated copy of the value specifically for the function, protecting the original variable from modification", "The function crashes"], correctAnswer: 2 },
                    { question: "How can a custom function directly manipulate variables defined remotely inside `main()`?", options: ["By utilizing `System.overwrite()`", "By declaring them universally global dynamically", "By exclusively relying on the `scanf` utility", "By demanding physical memory addresses (Pointers) as arguments, allowing the function to reach across RAM via Dereferencing"], correctAnswer: 3 }
                ]
            }
        ]
},
{
    title: "Mini Projects — Functions", order: 2,
        nodes: [
            {
                title: "Project Guide: Prime Number Checker", type: "lesson", order: 1,
                content: `## Project Guide: Prime Number Checker Algorithm\n\n### Introduction\nThe calculation of Prime Numbers acts as the foundational core of modern cryptography and cybersecurity protocols. In this capstone project for Section 5, you will construct an infinitely looped analytical tool that evaluates variables to determine primality. Crucially, strictly orchestrating the mathematical workload into a cleanly abstracted custom C function.\n\n### Goal\nArchitect a program containing an isolated \`is_prime\` function that strictly returns an internal status code (1 for true, 0 for false). The \`main\` framework should solicit the user for an integer eternally via a \`while\` loop, route the numerical data to the custom function reliably, evaluate the returned status code using \`if\` operations, and format the analytical output aesthetically.\n\n### Requirements\n1. Establish a prototype for an \`int is_prime(int number)\` function at the absolute ceiling of the file.\n2. In \`main()\`, deploy an infinite interactive \`while\` loop that prompts for a dynamically inputted integer.\n3. Implement a termination mechanism: if the user supplies a \`0\` or a negative structure, instantly sever the loop.\n4. Route the integer into the mathematical function.\n5. Design the function's analytical engine: a prime number is defined as explicitly larger than 1 and entirely indivisible by any digit aside from 1 and itself.\n6. Within the function, utilize a \`for\` loop ascending from $2$ up to half of the targeted number. If any evaluated divisor produces a \`0\` remainder via the modulo \` % \` operator, instantly return False (\`0\`).\n7. If the loop completes successfully, cleanly return True (\`1\`).\n\n### Step-by-Step Guide\n\n**Step 1: The Blueprint Prototype**\nBelow \`#include <stdio.h>\`, type \`int is_prime(int n); \`.\n\n**Step 2: Designing the Engine**\nAt the terminus of the file, fabricate the function logic. \`int is_prime(int n) {
                    \` \nInitially screen the integer: \`if (n <= 1) return 0; \` \nStructure the investigative cycle: \`for (int i = 2; i <= n / 2; i++) {
                        \`. \nInduce the modulo check: \`if (n % i == 0) return 0; \`. If encountering an exact division, it is absolutely not prime, efficiently stopping mathematical rendering immediately.\nIf the loop gracefully terminates unhindered, submit a clean \`return 1; \` at the absolute conclusion.\n\n**Step 3: The Graphical Implementation**\nWithin \`main()\`, integrate \`while (1) {
                            \` to enforce an immutable perpetual state. \nSolicit an integer via \`scanf("%d", & check_num); \` \nAssemble conditions: \`if (check_num <= 0) break; \` \nRetrieve the functional value: \`int result = is_prime(check_num); \` \nTighten an \`if/else\` response block conditionally evaluating whether \`result\` mathematically equals \`1\`, printing out the formatted cryptographic verdict.\n\n### Challenge Yourself\n- The internal \`for\` loop check utilizing \`n / 2\` is terribly inefficient for massive cryptography numbers dynamically. Import the \`<math.h>\` architecture and optimize the loop terminator to explicitly utilize the fundamental square root: \`int max = sqrt(n); \`! (You will need to pass \` - lm\` to GCC to successfully link the specialized math binary library).`
                        }
        ]
                    }
    ]
    }
];
