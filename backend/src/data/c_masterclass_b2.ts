// C Masterclass — Part B2 (Sections 8-9)
export const cMasterclassPartB2 = [
    {
        title: "File Handling and Dynamic Memory", order: 8,
        chapters: [
            {
                title: "File Streams in C", order: 1,
                nodes: [
                    {
                        title: "Reading and Writing Files", type: "lesson", order: 1,
                        content: `## Reading and Writing Files in C\n\n### The Necessity of Persistence\nWhen a C program successfully terminates, the operating system aggressively reclaims all RAM allocated to it. Every massive Array, every intricate Struct, and every deeply calculated variable vanishes into the void permanently. To save analytics, game states, or configurations, you must enforce persistence. You do this by creating a physical text file dynamically on the local hard drive.\n\nBecause C dictates memory operations manually, file manipulation operates uniquely using explicit "Streams." A file stream acts as a highly optimized conduit connecting your raw RAM directly to the physical storage partition.\n\n### The FILE Pointer\nTo interface securely with an operating system file, you must initialize an explicit \`FILE\` pointer. This specialized pointer targets the memory structure governing the file stream natively.\n\n\`\`\`c\n#include <stdio.h>\n\nint main() {\n    // Initialize the pointer specifically for File operations\n    FILE *fileHandler;\n    \n    // Establish the connection. 'w' stands explicitly for Write Mode.\n    // WARNING: 'w' aggressively overwrites any existing file carrying that name natively!\n    fileHandler = fopen("database.txt", "w"); \n\n    if (fileHandler == NULL) {\n        printf("FATAL ERROR: Insufficient permissions to create file.\\n");\n        return 1;\n    }\n    \n    // Transmit formatted data directly utilizing fprintf\n    fprintf(fileHandler, "User: Admin\\nStatus: Active\\n");\n    \n    // CRITICAL REQUIREMENT: Always sever the stream to mitigate corruption.\n    fclose(fileHandler);\n    return 0;\n}\n\`\`\`\n\n### Extracting File Data\nTo successfully read a file, utilize exactly the \`fopen\` protocol, but modify the access mode to \`'r'\` (Read Mode). Extraction requires an iterative string loop because C lacks robust dynamic string primitives implicitly.\n\n\`\`\`c\nFILE *reader = fopen("database.txt", "r");\nchar buffer[150]; // Create an explicit memory buffer to handle incoming strings.\n\n// fgets smoothly grabs precisely one mathematical line of strings per loop dynamically.\nwhile (fgets(buffer, sizeof(buffer), reader) != NULL) {\n    printf("Extracted Line: %s", buffer);\n}\nfclose(reader);\n\`\`\`\nWithout forcefully executing \`fclose()\`, the file remains firmly locked mechanically by the CPU, resulting in a severe memory leak and preventing independent processes from actively interacting with it natively.`
                    },
                    {
                        title: "Dynamic Memory Allocation", type: "lesson", order: 2,
                        content: `## Dynamic Memory Allocation (malloc, calloc)\n\n### The Problem with Static Arrays\nUp until now, we have strictly engineered "Static Arrays." \`int numbers[50];\`. You explicitly and aggressively dictate the capacity limits during compilation. But what if you are coding a Text Editor program? You have absolutely no concept natively of how many characters the user will type into the document. If you allocate \`char document[100]\`, the software explodes violently the moment they type the 101st character. If you allocate \`char document[999999999]\`, you consume astronomically grotesque amounts of RAM, angering the user and causing systems to severely slow down.\n\nYou need memory that flawlessly expands on demand seamlessly while the application is fundamentally running. You need **Dynamic Memory Allocation**.\n\n### Accessing the Heap\nStatic variables are explicitly allocated into a hyper-optimized and restrictive memory sector known as the **Stack**. Dynamic variables conversely are allocated logically into an expansive, chaotic sea of memory known structurally as the **Heap**. \n\nTo harvest bytes from the Heap on the fly dynamically, securely import \`<stdlib.h>\` and utilize the legendary \`malloc()\` function (Memory Allocate).\n\n\`\`\`c\n#include <stdio.h>\n#include <stdlib.h>\n\nint main() {\n    int user_capacity;\n    printf("How many integers must we securely store natively? ");\n    scanf("%d", &user_capacity);\n    \n    // We are violently extracting custom amounts of RAM on the fly!\n    // malloc forcefully requests the precise amount of internal bytes required dynamically.\n    int *dynamic_array = malloc(user_capacity * sizeof(int));\n    \n    if (dynamic_array == NULL) {\n        printf("MEMORY ALLOCATION FAILED CATASTROPHICALLY.\\n");\n        return 1;\n    }\n    \n    // Use the pointer EXACTLY as if it were a totally normal static array natively!\n    dynamic_array[0] = 42;\n    printf("Value stored in Heap Memory: %d\\n", dynamic_array[0]);\n    \n    return 0;\n}\n\`\`\`\n\n### Calloc: The Clean Architecture\nThe \`malloc\` protocol is incredibly fast, but terribly dangerous: it violently leaves whatever random garbage 1s and 0s previously existed fundamentally sitting within the extracted byte blocks natively.\n\nIf you prefer to securely inject \`0\` into every single allocated compartment guaranteeing pristine structures natively, utilize \`calloc()\` instead. The architectural footprint explicitly separates quantities from byte sizes internally:\n\n\`\`\`c\n// Acquires identically 100 integer compartments but forcefully erases them natively to zero.\nfloat *pristine_data = calloc(100, sizeof(float)); \n\`\`\``
                    },
                    {
                        title: "Memory Leaks and Free()", type: "lesson", order: 3,
                        content: `## Memory Leaks and Free()\n\n### The Curse of Maximum Power\nThe defining architectural superiority of the C Language inherently lies in absolute, unrestrained access natively granting the programmer flawless control securely over computer RAM explicitly. \n\nHowever, tremendous capabilities strictly mandate astronomical levels of manual responsibility inherently.\n\nWhen a standard C local variable naturally concludes securely within a function structure, the C Compiler natively deletes it proactively from the internal Stack seamlessly. You literally do nothing manually to handle it proactively.\n\nBut whenever you aggressively rip memory chunks directly off the Heap explicitly via the \`malloc()\` or \`calloc()\` pipelines natively, the C compiler fundamentally washes its hands of the situation entirely. It assumes you uniquely are a brilliant architect natively, and therefore explicitly demands you aggressively return the memory locally whenever you technically conclude operational processing natively.\n\n### The Deadly Memory Leak\nIf you algorithmically allocate a block fundamentally executing \`malloc(50000)\` actively within a repeating \`while\` loop natively, and you accidentally forget to erase it proactively, you have mathematically engineered a **Memory Leak**. \n\nThe operating system natively registers that RAM identically mapped to your application logically. With every single loop cycle seamlessly generated, another 50,000 bytes aggressively disappear into your inaccessible Heap structure inherently. Over minutes natively or specifically hours, your application will violently consume 100% of the entire terminal's RAM natively. The system will spectacularly crash explicitly utilizing a catastrophic Out-Of-Memory protocol inherently.\n\n### Enforcing the free() Protocol\nEvery single instance implicitly generating a \`malloc()\` locally must mathematically be paired perfectly utilizing the \`free()\` command naturally once the operational life cycle concludes internally.\n\n\`\`\`c\nvoid generate_report() {\n    // 1. Aggressively acquire massive data chunks natively.\n    int *massive_matrix = malloc(1000000 * sizeof(int));\n    \n    // 2. Perform analytical calculations heavily explicitly.\n    massive_matrix[0] = 55;\n    massive_matrix[999999] = 23;\n    \n    // 3. Mathematical objective explicitly achieved successfully natively.\n    \n    // 4. CRITICAL: Forcibly surrender the massive blocks dynamically securely!\n    free(massive_matrix);\n}\n\`\`\`\n\nThe \`free()\` protocol implicitly detaches the Pointer aggressively from the Heap Memory sector directly, returning those specific bytes natively to the Operating System pool seamlessly. \n\nWARNING: Once you naturally deploy \`free()\` effectively, technically attempting structurally to read dynamically the internal Pointer values aggressively natively generates a terminal Undefined Behavior crash explicitly!`
                    },
                    {
                        title: "Dynamic Systems Quiz", type: "quiz", order: 4,
                        questions: [
                            { question: "When natively generating persistence architectures logically utilizing the `fopen` function exclusively, what explicit datatype naturally returns technically?", options: ["A purely string array", "An explicit FILE Pointer identically managing the file stream mechanics safely", "A struct", "An int"], correctAnswer: 1 },
                            { question: "Why explicitly must a developer inherently actively engage `malloc` or `calloc` architecture cleanly?", options: ["To natively generate graphic user interfaces", "Because standard arrays explicitly mandate rigid hardcoded sizes physically during early compilation. Dynamic logic forcefully expands variables smoothly without pre-computation successfully", "Because it encrypts structurally the logic successfully", "To natively increase compilation acceleration physically"], correctAnswer: 1 },
                            { question: "What is the structural technical difference intrinsically operating between explicit `malloc()` natively and `calloc()` specifically?", options: ["malloc deletes variables, calloc stores variables seamlessly", "calloc works explicitly with arrays identically while malloc naturally executes structs exclusively", "malloc leaves identical preexisting raw garbage data organically in the physical memory explicitly, whereas calloc natively violently erases the acquired compartments securely to absolute Zero natively", "malloc consumes zero CPU explicitly, calloc heavily generates processor lag inherently"], correctAnswer: 2 },
                            { question: "What fundamentally occurs technically within an operating system identically if a developer heavily relies actively on `malloc()` but permanently fails natively explicitly commanding `free()` locally?", options: ["The compiler proactively generates a warning fundamentally", "A catastrophic Memory Leak emerges effectively, perpetually consuming identical RAM explicitly until the ultimate native container crashes entirely and radically out of resources natively", "The loop terminates violently securely", "The memory seamlessly writes itself proactively natively onto explicit hard drives completely"], correctAnswer: 1 }
                        ]
                    }
                ]
            },
            {
                title: "Mini Projects — Dynamic Architecture", order: 2,
                nodes: [
                    {
                        title: "Project Guide: Dynamic Contact Manager", type: "lesson", order: 1,
                        content: `## Project Guide: Dynamic Memory Roster Tool\n\n### Introduction\nExplicitly defining a text struct explicitly arrayed rigidly fundamentally limiting completely limits scalability heavily statically. In this project actively, you structurally inherently merge natively C concepts heavily explicitly. You natively architect absolutely explicitly structurally \`typedef struct\` architectures seamlessly managed heavily explicitly via heavily Dynamic Memory structurally allocated safely.\n\n### Goal\nConstruct natively securely interactive effectively database structures exclusively dynamically generating explicitly arrays utilizing \`malloc()\` inherently proactively smoothly based dynamically upon explicit keyboard metrics implicitly. Fill logically arrays with explicitly inherently requested User profiles effectively heavily. Print formatting exclusively natively output fundamentally. Sever logically memory ties fully comprehensively deploying absolutely securely \`free()\` effectively.\n\n### Requirements\n1. Define absolutely explicitly structurally \`typedef struct\` named explicit \`Contact\`. Internally natively equip fundamentally \`char name[50];\` strictly explicitly natively seamlessly an \`int rating;\`.\n2. In \`main()\`, implicitly prompt fundamentally executing user specifically effectively requesting natively: "How strictly actively structurally many profiles heavily to logically create intrinsically natively?"\n3. Capture explicitly inherently the input \`int quantity;\` purely.\n4. Aggressively request natively dynamic RAM essentially smoothly allocating explicitly a comprehensive Pointer explicitly targeting logically the \`Contact\` structural footprint purely specifically effectively scaled mathematically natively identical explicitly via the explicit quantity heavily.\n5. Integrate actively autonomous entirely \`for\` naturally loops natively essentially implicitly cycling exactly successfully to gather explicitly native natively requested profile data exclusively structurally natively into successfully dynamic explicit RAM blocks gracefully essentially.\n6. Produce natively explicitly secondary formatted loop purely outputting essentially successfully cleanly variables securely logically. \n7. Conclude absolutely physically enforcing safely inherently \`free()\` native protocols safely erasing dynamic structures mathematically successfully natively entirely.\n\n### Step-by-Step Guide\n\n**Step 1: Architecting Structure natively**\nAt explicitly peak formatting, define structurally natively explicitly global \`typedef struct { char name[50]; int rating; } Contact;\` natively essentially natively exclusively securely. \n\n**Step 2: The Core Acquisition Mechanism explicitly natively**\nSolicit efficiently actively: \`printf("Capacity required implicitly: ");\` specifically essentially. Extract fundamentally seamlessly natively: \`scanf("%d", &size);\` strictly cleanly successfully.\nCompute explicitly actively structural Memory securely natively: \`Contact *database = malloc(size * sizeof(Contact));\` exclusively natively purely seamlessly heavily heavily explicitly naturally.\n\n**Step 3: Populating strictly dynamically safely**\nDeploy successfully essentially efficiently loop successfully \`for(int i = 0; i < size; i++)\`. purely efficiently cleanly Prompt natively heavily: \`scanf("%s", database[i].name);\` explicitly seamlessly effectively. Prompt purely successfully natively \`scanf("%d", &database[i].rating);\` cleanly natively essentially securely natively.\n\n**Step 4: Secure Data Evacuation efficiently cleanly**\nVerify heavily cleanly gracefully utilizing \`for\` sequence naturally explicitly successfully \`printf\` completely essentially formatted natively implicitly structure effectively successfully dynamically strings seamlessly. \nAt absolute terminus securely heavily terminate structurally explicitly naturally deploying natively safely \`free(database);\` completely implicitly effectively exclusively securely successfully natively exclusively.\n\n### Challenge Yourself\n- Construct completely seamlessly effectively highly advanced gracefully conditionally robust structure strictly inherently leveraging exclusively \`realloc()\` fundamentally seamlessly! Allow securely dynamically users naturally successfully explicitly inject flawlessly native uniquely highly single instances fundamentally securely organically natively incrementally expanding arrays organically safely flawlessly seamlessly unconditionally.`
                    }
                ]
            }
        ]
    },

    {
        title: "Advanced Software Architecture", order: 9,
        chapters: [
            {
                title: "Multi-File Codebases", order: 1,
                nodes: [
                    {
                        title: "Header Files and Linking", type: "lesson", order: 1,
                        content: `## Header Files and Linking\n\n### The Monolithic Problem\nWhen your C program expands beyond 1,000 lines, maintaining a single \`main.c\` file becomes computationally disastrous and virtually impossible for developer teams to collaborate on concurrently. To engineer scalable systems safely, you must physically separate your architecture across multiple discrete files explicitly.\n\n### Separation of Concerns\nIn C, multi-file execution strictly demands splitting logic securely into two distinct file types:\n1. **Header Files (\`.h\`)**: These contain strictly Definitions. Struct Blueprints, Constants, and crucially, Function Prototypes.\n2. **Implementation Files (\`.c\`)**: These contain the literal, executed Mathematical Code Blocks (the actual functions).\n\n\`\`\`c\n// utils.h (The Header blueprint)\n#ifndef UTILS_H\n#define UTILS_H\n\n// Prototype uniquely advertised to the compiler\nint calculate_damage(int strength, int weapon_base);\n\n#endif\n\`\`\`\nThe \`#ifndef\` architecture natively prevents catastrophic "Double Inclusion" bugs structurally explicitly.\n\n### Connecting Modules safely\nInside your fundamental \`main.c\` structure explicitly, you safely import the Custom Header natively utilizing explicitly double quotes rather than strictly angular brackets!\n\n\`\`\`c\n// main.c (The Execution file)\n#include <stdio.h>\n#include "utils.h" // We import our local Header securely!\n\nint main() {\n    int total = calculate_damage(10, 5);\n    printf("Damage: %d", total);\n    return 0;\n}\n\`\`\`\n\n### The Compilation Phase\nWhen compiling multi-file architectures, you absolutely cannot securely utilize \`gcc main.c\`. You must explicitly securely command the GCC engine to securely harvest explicitly every single \`.c\` file comprehensively:\n\`gcc main.c utils.c -o game.exe\`\n\nThe compiler dynamically links the memory footprints automatically seamlessly.`
                    },
                    {
                        title: "Architecture Quiz", type: "quiz", order: 2,
                        questions: [
                            { question: "What explicit architectural role do Header (`.h`) files perform successfully natively?", options: ["They contain the main function execution natively", "They explicitly securely house Function Prototypes, Struct definitions, and Constants efficiently to logically advertise architecture safely securely", "They explicitly execute dynamically compiled output", "They strictly overwrite database memory seamlessly"], correctAnswer: 1 },
                            { question: "How specifically must you explicitly import cleanly safely a custom locally engineered Header explicitly compared natively reliably to standard libraries?", options: ["Using brackets: `#include <local.h>`", "Using explicit double quotes: `#include \"local.h\"`", "Using hashtags exclusively", "Using dynamic links strictly"], correctAnswer: 1 }
                        ]
                    },
                    {
                        title: "Project Guide: Multi-File Manager", type: "lesson", order: 3,
                        content: `## Project Guide: Multi-File Compilation System\n\n### Introduction\nTo finalize your mastery natively of foundational C Architecture cleanly securely, explicitly structurally segregate mathematical logic flawlessly across entirely separate text files cleanly reliably securely.\n\n### Goal\nConstruct a natively flawlessly split C Application creatively. Build specifically \`math_engine.h\` containing explicitly function prototypes natively efficiently cleanly. Build \`math_engine.c\` containing explicitly the functional algorithmic blocks cleanly smoothly cleanly. Build \`main.c\` containing explicitly successfully the core execution securely logically safely safely securely executing exactly \`gcc\`.`
          }
        ]
    }
]
  }
];
